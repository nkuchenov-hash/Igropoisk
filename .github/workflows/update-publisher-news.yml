name: Manual alias — update official gaming sources

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pipeline:
    uses: ./.github/workflows/news-pipeline.yml
    with:
      force: true
    secrets: inherit
