name: Build Игропоиск review

on:
  workflow_dispatch:
    inputs:
      slug:
        description: Exact game slug
        required: true
  push:
    branches: [staging]
    paths:
      - 'data/review-build-requests/*.json'

permissions:
  contents: write
  pull-requests: write
  models: read

concurrency:
  group: review-staging-${{ github.event_name }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          ref: staging
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Resolve requested game slug
        id: target
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            SLUG="${{ inputs.slug }}"
          else
            BEFORE="${{ github.event.before }}"
            AFTER="${{ github.sha }}"
            REQUEST_FILE="$(git diff --name-only "$BEFORE" "$AFTER" -- 'data/review-build-requests/*.json' | tail -n 1)"
            if [ -z "$REQUEST_FILE" ]; then
              REQUEST_FILE="$(git log -1 --format= --name-only "$AFTER" -- 'data/review-build-requests/*.json' | sed '/^$/d' | tail -n 1)"
            fi
            if [ -z "$REQUEST_FILE" ] || [ ! -f "$REQUEST_FILE" ]; then
              echo "Could not resolve review request from triggering commit $AFTER" >&2
              exit 2
            fi
            SLUG=$(node -e "const fs=require('fs');const d=JSON.parse(fs.readFileSync(process.argv[1],'utf8'));if(!d.slug)process.exit(2);process.stdout.write(d.slug)" "$REQUEST_FILE")
            echo "Resolved request file: $REQUEST_FILE"
          fi
          echo "slug=$SLUG" >> "$GITHUB_OUTPUT"
          echo "Building review for $SLUG"
      - name: Validate JavaScript
        run: |
          node --check scripts/sync-review-policy.mjs
          node --check scripts/prepare-review-research.mjs
          node --check scripts/discover-review-media.mjs
          node --check scripts/synthesize-review-github-models.mjs
          node --check scripts/enrich-review-media-github-models.mjs
          node --check scripts/validate-review-output.mjs
          node --check scripts/render-review-pages.mjs
          node --check scripts/canonicalize-editorial-game-id.mjs
          node --check scripts/lib/editorial-game-registry-adapter.mjs
          node --check article/_shared/review-carousel.js
      - name: Synchronize text, media and layout gates
        run: node scripts/sync-review-policy.mjs
      - name: Discover or revalidate professional sources
        run: node scripts/prepare-review-research.mjs "${{ steps.target.outputs.slug }}"
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ vars.OPENAI_MODEL || 'gpt-5' }}
          OPENAI_RESEARCH_MODEL: ${{ vars.OPENAI_RESEARCH_MODEL || 'gpt-5' }}
      - name: Bind research to canonical Game Registry identity
        run: node scripts/canonicalize-editorial-game-id.mjs "${{ steps.target.outputs.slug }}"
      - name: Verify official game-specific screenshot corpus
        run: node scripts/discover-review-media.mjs "${{ steps.target.outputs.slug }}"
      - name: Write game-specific article through GitHub Models
        run: node scripts/synthesize-review-github-models.mjs "${{ steps.target.outputs.slug }}"
        env:
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REVIEW_MODEL: openai/gpt-4.1
      - name: Bind synthesized artifacts to canonical Game Registry identity
        run: node scripts/canonicalize-editorial-game-id.mjs "${{ steps.target.outputs.slug }}"
      - name: Match unique verified screenshots to article sections
        run: node scripts/enrich-review-media-github-models.mjs "${{ steps.target.outputs.slug }}"
        env:
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REVIEW_MODEL: openai/gpt-4.1
          GITHUB_VISION_MODEL: openai/gpt-4.1
      - name: Enforce article depth, dimensions and zero duplicates
        run: node scripts/validate-review-output.mjs "${{ steps.target.outputs.slug }}"
      - name: Re-bind generated media/report artifacts to canonical identity
        run: node scripts/canonicalize-editorial-game-id.mjs "${{ steps.target.outputs.slug }}"
      - name: Render static pages for every published review
        run: node scripts/render-review-pages.mjs
      - name: Publish audited review through isolated PR
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          slug="${{ steps.target.outputs.slug }}"
          git add config/parsers/review-synthesis.json data/research data/reviews data/media-candidates data/articles data/article-drafts data/article-media data/parser-runs data/drafts article
          if git diff --cached --quiet; then
            echo "No audited review changes to publish."
            exit 0
          fi
          git diff --cached --check
          branch="automation/review-${slug}-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          git config user.name "igropoisk-editor[bot]"
          git config user.email "igropoisk-editor[bot]@users.noreply.github.com"
          git checkout -b "$branch"
          git commit -m "Build audited review for ${slug}"
          git push origin "$branch"
          pr_url="$(gh pr create \
            --base staging \
            --head "$branch" \
            --title "Audited review: ${slug}" \
            --body "Generated only after the configured Review Synthesizer passed source, identity, article-depth, media-deduplication and visual-quality gates for ${slug}. Existing complete professional source packs are revalidated without paid discovery; article text and visual matching use GitHub Models with repository-scoped Actions permissions. The workflow never writes review content directly to staging.")"
          gh pr merge "$pr_url" --merge --delete-branch
