name: Five-game strict model capability rerun

on:
  push:
    branches: [benchmark/five-games-capability-rerun-20260904]
    paths:
      - '.github/workflows/five-games-capability-rerun.yml'
      - 'scripts/run-editorial-capability-one-model.mjs'
      - 'scripts/summarize-editorial-capability.mjs'
      - 'benchmarks/editorial-five-games/games.json'
  workflow_dispatch:

permissions:
  contents: read

env:
  NODE_NO_WARNINGS: '1'

jobs:
  assemble:
    name: Freeze identical pack — ${{ matrix.game }}
    runs-on: ubuntu-latest
    timeout-minutes: 35
    strategy:
      fail-fast: false
      max-parallel: 5
      matrix:
        game:
          - mafia
          - dangerous-dave-in-the-haunted-mansion
          - far-cry
          - jack-orlando-a-cinematic-adventure-1997
          - mass-effect-2007
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Enable universal semantic vocabulary
        run: node scripts/patch-benchmark-game-knowledge-vocabulary.mjs
      - name: Enable configured factual source seeds
        run: node scripts/patch-dangerous-dave-benchmark-source-seeds.mjs
      - name: Build frozen post-assembly pack once
        run: node scripts/build-editorial-benchmark-pack.mjs "${{ matrix.game }}"
      - name: Validate source depth and identity
        env:
          GAME: ${{ matrix.game }}
        run: |
          node - <<'NODE'
          const fs=require('fs');
          const crypto=require('crypto');
          const slug=process.env.GAME;
          const file=`benchmark-packs/${slug}/pack.json`;
          const raw=fs.readFileSync(file);
          const p=JSON.parse(raw);
          if(p.assembly?.source_scan_complete!==true) throw new Error('source scan incomplete');
          if(Number(p.assembly?.defining_claims||0)<4) throw new Error('fewer than 4 defining claims');
          if(Number(p.assembly?.readable_sources||0)<3) throw new Error('fewer than 3 readable sources');
          const identityText=JSON.stringify({display_title:p.game?.display_title,identity_title:p.game?.identity_title,knowledge:p.game_knowledge,material:p.readable_source_material});
          const corpusText=JSON.stringify({knowledge:p.game_knowledge,material:p.readable_source_material});
          const required={
            mafia:/mafia|tommy|salieri|lost\s+heaven/i,
            'dangerous-dave-in-the-haunted-mansion':/dangerous\s+dave|haunted\s+mansion|delbert|shotgun/i,
            'far-cry':/far\s+cry|jack\s+carver|crytek|tropical/i,
            'jack-orlando-a-cinematic-adventure-1997':/jack\s+orlando|detective|adventure/i,
            'mass-effect-2007':/mass\s+effect|shepard|citadel|bioware/i
          };
          if(!required[slug]?.test(identityText)) throw new Error(`identity not established for ${slug}`);
          if(slug==='dangerous-dave-in-the-haunted-mansion'&&/doom\s*64|munchkin/i.test(corpusText)) throw new Error('Dangerous Dave cross-game contamination');
          const sha=crypto.createHash('sha256').update(raw).digest('hex');
          fs.writeFileSync(`benchmark-packs/${slug}/PACK_SHA256.txt`,sha+'\n');
          console.log(JSON.stringify({game:slug,readable_sources:p.assembly.readable_sources,defining_claims:p.assembly.defining_claims,pack_sha256:sha},null,2));
          NODE
      - uses: actions/upload-artifact@v4
        with:
          name: capability-pack-${{ matrix.game }}
          path: benchmark-packs/${{ matrix.game }}
          if-no-files-found: error
          retention-days: 14

  model:
    name: ${{ matrix.game }} — ${{ matrix.candidate.label }}
    needs: assemble
    runs-on: ubuntu-latest
    timeout-minutes: 40
    continue-on-error: true
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        game:
          - mafia
          - dangerous-dave-in-the-haunted-mansion
          - far-cry
          - jack-orlando-a-cinematic-adventure-1997
          - mass-effect-2007
        candidate:
          - { id: gigachat-3-ultra, label: 'GigaChat 3 Ultra', provider: gigachat, model: 'GigaChat-3-Ultra' }
          - { id: gemini-3-8-flash, label: 'Gemini 3.8 Flash', provider: gemini, model: 'gemini-3.8-flash' }
          - { id: gemini-3-7-flash, label: 'Gemini 3.7 Flash', provider: gemini, model: 'gemini-3.7-flash' }
          - { id: qwen3-8-27b, label: 'Qwen 3.8 27B', provider: groq, model: 'qwen/qwen3.8-27b' }
          - { id: qwen3-6-27b, label: 'Qwen 3.6 27B', provider: groq, model: 'qwen/qwen3.6-27b' }
          - { id: gpt-oss-120b, label: 'GPT-OSS 120B', provider: groq, model: 'openai/gpt-oss-120b' }
          - { id: gpt-oss-20b, label: 'GPT-OSS 20B', provider: groq, model: 'openai/gpt-oss-20b' }
          - { id: glm-5-2-free, label: 'GLM 5.2 Free', provider: openrouter, model: 'z-ai/glm-5.2:free' }
          - { id: nemotron-3-ultra-free, label: 'Nemotron 3 Ultra Free', provider: openrouter, model: 'nvidia/nemotron-3-ultra-550b-a55b:free' }
          - { id: nemotron-3-super-free, label: 'Nemotron 3 Super Free', provider: openrouter, model: 'nvidia/nemotron-3-super-120b-a12b:free' }
          - { id: minimax-m2-7-free, label: 'MiniMax M2.7 Free', provider: openrouter, model: 'minimax/minimax-m2.7:free' }
          - { id: minimax-m3-free, label: 'MiniMax M3 Free', provider: openrouter, model: 'minimax/minimax-m3:free' }
          - { id: dots3-note-free, label: 'Dots3-Note Preview Free', provider: openrouter, model: 'dots-studio/dots-3-note-preview:free' }
          - { id: gemma-4-31b-free, label: 'Gemma 4 31B Free', provider: openrouter, model: 'google/gemma-4-31b-it:free' }
          - { id: gemma-4-26b-a4b-free, label: 'Gemma 4 26B A4B Free', provider: openrouter, model: 'google/gemma-4-26b-a4b-it:free' }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/download-artifact@v4
        with:
          name: capability-pack-${{ matrix.game }}
          path: benchmark-packs/${{ matrix.game }}
      - name: Run same-model production retry policy
        env:
          CAPABILITY_ATTEMPTS: '3'
          BENCH_GAME: ${{ matrix.game }}
          BENCH_ID: ${{ matrix.candidate.id }}
          BENCH_LABEL: ${{ matrix.candidate.label }}
          BENCH_PROVIDER: ${{ matrix.candidate.provider }}
          BENCH_MODEL: ${{ matrix.candidate.model }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          OPEN_ROUTER_API_KEY: ${{ secrets.OPEN_ROUTER_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GOOGLE_AI_API_KEY: ${{ secrets.GOOGLE_AI_API_KEY }}
          GOOGLE_GEMINI_API_KEY: ${{ secrets.GOOGLE_GEMINI_API_KEY }}
          GIGACHAT_AUTH_KEY: ${{ secrets.GIGACHAT_AUTH_KEY }}
          GIGACHAT_CREDENTIALS: ${{ secrets.GIGACHAT_CREDENTIALS }}
          GIGACHAT_API_KEY: ${{ secrets.GIGACHAT_API_KEY }}
          GIGACHAT_AUTHORIZATION_KEY: ${{ secrets.GIGACHAT_AUTHORIZATION_KEY }}
          GIGACHAT_KEY: ${{ secrets.GIGACHAT_KEY }}
        run: node scripts/run-editorial-capability-one-model.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: capability-result-${{ matrix.game }}-${{ matrix.candidate.id }}
          path: benchmark-capability
          if-no-files-found: error
          retention-days: 14

  summarize:
    name: Binary capability summary
    needs: [assemble, model]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/download-artifact@v4
        with:
          pattern: capability-result-*
          path: capability-artifacts
          merge-multiple: false
      - name: Prove same frozen pack per game and classify models
        run: node scripts/summarize-editorial-capability.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: five-game-capability-summary
          path: capability-summary
          if-no-files-found: error
          retention-days: 14
