name: Manual alias — update gaming news

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pipeline:
    uses: ./.github/workflows/news-pipeline.yml
    with:
      force: true
    secrets: inherit
