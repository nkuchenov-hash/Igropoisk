#!/usr/bin/env node
import fs from 'node:fs';
const read=path=>fs.readFileSync(path,'utf8');
const creator=read('scripts/ensure-game-page.mjs'),verified=read('scripts/lib/verified-game-import.mjs'),series=read('scripts/materialize-known-series.mjs'),runner=read('scripts/run-game-post-create-enrichment.mjs'),publisher=read('scripts/publish-game-post-create-overlay.mjs'),media=read('scripts/enrich-game-media-from-sources.mjs'),runtime=read('game/_shared/game-page-materialized-data.js'),verifiedWorkflow=read('.github/workflows/verified-game-import-fast.yml'),newsWorkflow=read('.github/workflows/news-game-page-fast.yml'),enrichmentWorkflow=read('.github/workflows/game-post-create-enrichment.yml');
const fail=message=>{throw new Error(message)};
if(!creator.includes('series: series || null'))fail('Game Creator does not preserve canonical series');
if(!creator.includes('data/game-enrichment-requests/${slug}.json'))fail('Game Creator does not emit post-create enrichment request');
if(!creator.includes("reason: 'post_create_enrichment_must_not_block_base_page'"))fail('Post-create request does not document non-blocking boundary');
if(!creator.includes('reviewNeeded = released && !reviewReady'))fail('Released game without review is not an automatic review trigger');
if(!verified.includes('verified_series_attached'))fail('Matched verified imports still lose supplied series');
if(!series.includes('data/franchises/${slug}.json')||!series.includes('canonical_series_backfilled'))fail('Known series materialization/backfill contract missing');
if(!runner.includes("'scripts/calculate-ratings-from-research.mjs'")||!runner.includes("'scripts/quality-control-loop.mjs'")||!runner.includes("'scripts/enrich-game-media-from-sources.mjs'"))fail('Post-create rating/review/media stages missing');
if(runner.indexOf("'scripts/calculate-ratings-from-research.mjs'")>runner.indexOf("'scripts/quality-control-loop.mjs'"))fail('Rating bootstrap must happen before slower review build');
if(!runner.includes("phase!=='review'")||!runner.includes("phase!=='bootstrap'"))fail('Bootstrap/review phases not separated');
if(!runner.includes('Bootstrap deliberately leaves request files untouched'))fail('Bootstrap can self-trigger before review phase');
const materializeAt=runner.indexOf("run('catalog-materialization'");
const materializeContext=runner.slice(Math.max(0,materializeAt-1200),materializeAt);
if(materializeAt<0||!materializeContext.includes('if(doReview){'))fail('Catalog-wide materialization still runs during fast bootstrap');
if(!runner.includes("phase==='bootstrap'?'game-post-create-bootstrap.json'"))fail('Bootstrap still overwrites shared enrichment report');
if(!media.includes("provider:'verified-source-page'")||!media.includes('Search engines may aid human/source discovery'))fail('Verified media provenance/discovery policy missing');
if(!runtime.includes('renderEnrichedMedia(draft,title)'))fail('Enriched draft media is not exposed by runtime');
if(!/paths=\([^\n]*data\/game-enrichment-requests/.test(verifiedWorkflow)||!/paths=\([^\n]*data\/game-enrichment-requests/.test(newsWorkflow))fail('Fast creator wrapper discards enrichment requests');
if(!enrichmentWorkflow.includes("OPENAI_API_KEY: ''")||!enrichmentWorkflow.includes('qwen3:1.7b')||!enrichmentWorkflow.includes('qwen3-vl:4b'))fail('Local model fallbacks missing');
if(!enrichmentWorkflow.includes('cancel-in-progress: true'))fail('Fresh enrichment cannot cancel stale run');
const bootstrapAt=enrichmentWorkflow.indexOf('POST_CREATE_PHASE: bootstrap'),bootstrapPublishAt=enrichmentWorkflow.indexOf('POST_CREATE_PUBLISH_PHASE: bootstrap'),modelAt=enrichmentWorkflow.indexOf('Restore local editorial model cache'),reviewAt=enrichmentWorkflow.indexOf('POST_CREATE_PHASE: review');
if(bootstrapAt<0||bootstrapPublishAt<bootstrapAt||modelAt<bootstrapPublishAt||reviewAt<modelAt)fail('Fast checkpoint is not published before review synthesis');
if(!publisher.includes('POST_CREATE_PUBLISH_PHASE')||!publisher.includes("freshObj!==baseObj")||!publisher.includes('for(let publishAttempt=1;publishAttempt<=5;publishAttempt++)'))fail('Conflict-safe publisher contract missing');
if(!publisher.includes('createPrWithRetry')||!publisher.includes('Transient GitHub PR-create failure'))fail('Publisher does not retry transient GitHub PR creation failures');
console.log('Game Creator post-create contract passed: bootstrap is request-scoped, series/rating/media publish before reviews, global catalog materialization is review-only, and publication retries transient GitHub failures.');
