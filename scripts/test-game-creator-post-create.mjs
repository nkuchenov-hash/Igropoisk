#!/usr/bin/env node
import fs from 'node:fs';
const read=path=>fs.readFileSync(path,'utf8');
const creator=read('scripts/ensure-game-page.mjs');
const verified=read('scripts/lib/verified-game-import.mjs');
const series=read('scripts/materialize-known-series.mjs');
const runner=read('scripts/run-game-post-create-enrichment.mjs');
const publisher=read('scripts/publish-game-post-create-overlay.mjs');
const media=read('scripts/enrich-game-media-from-sources.mjs');
const reviewDiscovery=read('scripts/discover-review-sources-web.mjs');
const historicalDiscoveryV14=read('scripts/discover-review-sources-web-v14.mjs');
const criticDiscoveryV15=read('scripts/discover-review-sources-web-v15.mjs');
const postCreateResearch=read('scripts/prepare-post-create-review-research.mjs');
const reviewRegistry=read('scripts/lib/review-source-registry.mjs');
const reviewRegistryBase=read('config/parsers/review-source-registry.json');
const reviewRegistryExtra=read('config/parsers/review-source-registry.extra.json');
const reviewRegistryConfig=`${reviewRegistryBase}\n${reviewRegistryExtra}`;
const scoreExtractor=read('scripts/lib/review-score-extractor.mjs');
const quickReview=read('scripts/build-review-bootstrap-local.mjs');
const reviewFeed=read('scripts/materialize-review-publication-feed.mjs');
const runtime=read('game/_shared/game-page-materialized-data.js');
const verifiedWorkflow=read('.github/workflows/verified-game-import-fast.yml');
const newsWorkflow=read('.github/workflows/news-game-page-fast.yml');
const enrichmentWorkflow=read('.github/workflows/game-post-create-enrichment.yml');
const fail=message=>{throw new Error(message)};
function blockEnd(source,start){const open=source.indexOf('{',start);if(open<0)return-1;let depth=0;for(let i=open;i<source.length;i++){if(source[i]==='{')depth++;else if(source[i]==='}'&&--depth===0)return i}return-1}
if(!creator.includes('series: series || null'))fail('Game Creator does not preserve canonical series');
if(!creator.includes('data/game-enrichment-requests/${slug}.json'))fail('Game Creator does not emit post-create enrichment request');
if(!creator.includes("reason: 'post_create_enrichment_must_not_block_base_page'"))fail('Post-create request does not document non-blocking boundary');
if(!creator.includes('reviewNeeded = released && !reviewReady'))fail('Released game without review is not an automatic review trigger');
if(!verified.includes('verified_series_attached'))fail('Matched verified imports still lose supplied series');
if(!series.includes('data/franchises/${slug}.json')||!series.includes('canonical_series_backfilled'))fail('Known series materialization/backfill contract missing');
if(!runner.includes("'scripts/calculate-ratings-from-research.mjs'")||!runner.includes("'scripts/enrich-game-media-from-sources.mjs'"))fail('Fast rating/media enrichment stages missing');
if(!runner.includes("'scripts/prepare-post-create-review-research.mjs'"))fail('Post-create runner can still invoke the destructive general research expander');
if(runner.includes("await runAsync(`review-research:${slug}`,'scripts/prepare-review-research.mjs'"))fail('Post-create runner still allows general research to erase verified historical evidence');
if(!runner.includes("'scripts/build-review-bootstrap-local.mjs'")||!runner.includes("'scripts/quality-control-loop.mjs'"))fail('Quick/full review stages missing');
if(runner.indexOf("'scripts/build-review-bootstrap-local.mjs'")>runner.indexOf("'scripts/quality-control-loop.mjs'"))fail('Quick review must be generated before the heavy full-review upgrade');
if(!runner.includes('requestScoreGreen')||!runner.includes('Number(requestScoreGreen(b))-Number(requestScoreGreen(a))'))fail('Green-rated games are not prioritized for the bounded quick-review batch');
for(const marker of ["'quick-review'","'full-review'",'doQuickReview','doFullReview','finalizeRequests=doFullReview','Quick-review checkpoint is intentionally request-state neutral'])if(!runner.includes(marker))fail(`Independent quick/full review phase contract missing: ${marker}`);
if(!runner.includes("review:fullReady?'ready':quickReady?'bootstrap_ready'"))fail('Quick review state is not observable independently from full review');
if(!runner.includes('Bootstrap deliberately leaves request files untouched'))fail('Bootstrap can self-trigger before review phase');
const fullPhaseAt=runner.indexOf('if(doFullReview){');
const fullPhaseEnd=blockEnd(runner,fullPhaseAt);
const materializeAt=runner.indexOf("run('catalog-materialization'");
if(fullPhaseAt<0||fullPhaseEnd<0||materializeAt<=fullPhaseAt||materializeAt>=fullPhaseEnd)fail('Catalog-wide materialization is not confined to the slow full-review phase');
if(!runner.includes("phase==='bootstrap'?'game-post-create-bootstrap.json':phase==='quick-review'?'game-post-create-quick-review.json':phase==='full-review'?'game-post-create-full-review.json'"))fail('Independent post-create phase reports missing');
if(!media.includes("provider:'verified-source-page'")||!media.includes('search engines may aid source discovery')||!media.includes('removed_stale_screenshots')||!media.includes('media.url_template'))fail('Verified media provenance, registered-gallery, stale-cleanup or discovery policy missing');
for(const marker of ['/media/${slug}/','/gallery/${slug}-screenshots/','/gallery/${slug}-artwork/','facebook[-_]?thumb'])if(!media.includes(marker))fail(`Historical native media gallery contract missing: ${marker}`);
if(!reviewDiscovery.includes('discover-review-sources-web-v15.mjs'))fail('Canonical review discovery does not route through v15 critic index');
for(const marker of ['rpgFanCandidates','rpgamerCandidates','gameRevolutionCandidates'])if(!historicalDiscoveryV14.includes(marker))fail(`Historical review adapter missing: ${marker}`);
if(!historicalDiscoveryV14.includes('allowQueryPermalink:true'))fail('Legacy GameRevolution permalink support missing');
for(const marker of ['metacritic','historical-critic-index-attribution','critic-index-attribution','metascore_as_vote:false','user_scores_as_votes:false','professional_only:true','platformTokens','critic_index_platform','released'])if(!criticDiscoveryV15.includes(marker))fail(`Released-game critic-index policy missing: ${marker}`);
if(criticDiscoveryV15.includes("await import('./discover-review-sources-web-v14.mjs')"))fail('v15 still shares a top-level-await process with the legacy discovery stack');
if(criticDiscoveryV15.includes('if(!historical){'))fail('Critic index is still artificially disabled for modern released games');
for(const marker of ["spawnSync('node'","scripts/discover-review-sources-web-v14.mjs","scripts/discover-review-sources-web-v8.mjs",'REVIEW_LEGACY_STACK_TIMEOUT_MS','legacy_stack'])if(!criticDiscoveryV15.includes(marker))fail(`Isolated legacy-discovery fallback missing: ${marker}`);
for(const marker of ['isTrustedEditorialScore','historical_index_preserved','post_create_verified_corpus_preserved','metascore_as_vote:false','user_scores_as_votes:false'])if(!postCreateResearch.includes(marker))fail(`Post-create verified corpus preservation missing: ${marker}`);
if(!reviewRegistry.includes('user_generated_review')||!reviewRegistry.includes('от\\s+пользователя'))fail('User-generated review pages are not excluded from editorial corpus');
if(!scoreExtractor.includes("method==='historical-critic-index-attribution'")||!scoreExtractor.includes("method==='critic-index-attribution'")||!scoreExtractor.includes("evidence.index_source==='metacritic'")||!scoreExtractor.includes('evidence.aggregate_score_used!==true')||!scoreExtractor.includes('evidence.user_score_used!==true'))fail('Attributed critic-score evidence contract missing');
for(const marker of ['"gamestar"','"the-games-machine"','"cd-action"','"gamesbeat"','"gamezebo"'])if(!reviewRegistryConfig.includes(marker))fail(`Released-game professional source missing: ${marker}`);
if(!quickReview.includes("review_stage:'bootstrap'")||!quickReview.includes('minimum_independent_professional_sources:3'))fail('Quick review is not a separately identified, 3-source professional bootstrap');
if(!quickReview.includes("review?.review_score?.status!=='green'"))fail('Quick review can publish before canonical rating is green');
if(!reviewFeed.includes('bootstrapDesired')||!reviewFeed.includes("feed.igropoisk_article.review_stage==='full'"))fail('Full review feed materializer can erase a valid bootstrap review or cannot upgrade it');
if(!publisher.includes("'data/review-bootstrap'"))fail('Quick review artifact is not persisted by conflict-safe publisher');
if(!runtime.includes('renderEnrichedMedia(draft,title)'))fail('Enriched draft media is not exposed by runtime');
if(!/paths=\([^\n]*data\/game-enrichment-requests/.test(verifiedWorkflow)||!/paths=\([^\n]*data\/game-enrichment-requests/.test(newsWorkflow))fail('Fast creator wrapper discards enrichment requests');
if(!enrichmentWorkflow.includes("OPENAI_API_KEY: ''")||!enrichmentWorkflow.includes('qwen3:1.7b')||!enrichmentWorkflow.includes('qwen3-vl:4b'))fail('Local model routing missing');
for(const marker of ['pull_required','pull_optional','Required text model','Optional vision model','quick text review will continue'])if(!enrichmentWorkflow.includes(marker))fail(`Quick-review model resilience missing: ${marker}`);
if(enrichmentWorkflow.includes("ollama list | grep -q '^qwen3-vl:4b' || ollama pull qwen3-vl:4b"))fail('Optional vision model can still block quick review');
for(const marker of ['POST_CREATE_PHASE: quick-review','POST_CREATE_PUBLISH_PHASE: quick-review','POST_CREATE_PHASE: full-review','POST_CREATE_PUBLISH_PHASE: full-review'])if(!enrichmentWorkflow.includes(marker))fail(`Independent quick/full workflow checkpoint missing: ${marker}`);
const bootstrapAt=enrichmentWorkflow.indexOf('POST_CREATE_PHASE: bootstrap'),bootstrapPublishAt=enrichmentWorkflow.indexOf('POST_CREATE_PUBLISH_PHASE: bootstrap'),modelAt=enrichmentWorkflow.indexOf('Restore local editorial model cache'),quickAt=enrichmentWorkflow.indexOf('POST_CREATE_PHASE: quick-review'),quickPublishAt=enrichmentWorkflow.indexOf('POST_CREATE_PUBLISH_PHASE: quick-review'),fullAt=enrichmentWorkflow.indexOf('POST_CREATE_PHASE: full-review'),fullPublishAt=enrichmentWorkflow.indexOf('POST_CREATE_PUBLISH_PHASE: full-review');
if(bootstrapAt<0||bootstrapPublishAt<bootstrapAt||modelAt<bootstrapPublishAt||quickAt<modelAt||quickPublishAt<quickAt||fullAt<quickPublishAt||fullPublishAt<fullAt)fail('Quick review is not published before the heavy full-review phase');
if(!enrichmentWorkflow.includes('cancel-in-progress: true'))fail('Fresh enrichment cannot cancel stale run');
if(!publisher.includes('POST_CREATE_PUBLISH_PHASE')||!publisher.includes("freshObj!==baseObj")||!publisher.includes('for(let publishAttempt=1;publishAttempt<=5;publishAttempt++)'))fail('Conflict-safe publisher contract missing');
if(!publisher.includes('createPrWithRetry')||!publisher.includes('Transient GitHub PR-create failure'))fail('Publisher does not retry transient GitHub PR creation failures');
console.log('Game Creator post-create contract passed: green ratings are prioritized; quick reviews publish in their own checkpoint before independent heavy upgrades.');
