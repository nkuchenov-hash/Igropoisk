#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';

const root=process.cwd();
const slug=String(process.argv[2]||'').trim();
if(!slug)throw new Error('Usage: node scripts/discover-game-sources-web.mjs <game-slug>');
const read=(p,f=null)=>{try{return JSON.parse(fs.readFileSync(path.join(root,p),'utf8'))}catch{return f}};
const write=(p,v)=>{const t=path.join(root,p);fs.mkdirSync(path.dirname(t),{recursive:true});fs.writeFileSync(t,JSON.stringify(v,null,2)+'\n')};
const draft=read(`data/drafts/${slug}.json`);if(!draft?.identity?.title)throw new Error(`Missing draft for ${slug}`);
const config=read('config/parsers/review-synthesis.json',{}),quality=read('config/game-page-quality-v2.json',{}),policy=quality.game_source_corpus||{};
const title=String(draft.identity.title),year=Number(String(draft.release?.date||draft.release?.date_text||'').match(/(?:19|20)\d{2}/)?.[0]||0),minimum=Number(policy.minimum_professional_sources||10),minimumScored=Number(quality.rating?.minimum_sources||5),checkedAt=new Date().toISOString();
const decodeHtml=s=>String(s||'').replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#x27;|&#39;|&apos;/g,"'").replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/<[^>]*>/g,' ').replace(/\s+/g,' ').trim();
const canonical=v=>{try{const u=new URL(String(v||''),'https://duckduckgo.com');if(u.hostname.endsWith('duckduckgo.com')&&u.pathname.startsWith('/l/')){const t=u.searchParams.get('uddg');if(t)return canonical(decodeURIComponent(t))}if(/google\./i.test(u.hostname)&&u.pathname==='/url'){const t=u.searchParams.get('q')||u.searchParams.get('url');if(t)return canonical(t)}if(/yahoo\./i.test(u.hostname)&&/\/RU=/.test(u.pathname)){const m=u.pathname.match(/\/RU=([^/]+)/);if(m)try{return canonical(decodeURIComponent(m[1]))}catch{}}u.hash='';for(const k of ['utm_source','utm_medium','utm_campaign','utm_content','utm_term','ysclid'])u.searchParams.delete(k);return `${u.origin}${u.pathname.replace(/\/$/,'')}${u.search}`}catch{return''}};
const host=v=>{try{return new URL(v).hostname.replace(/^www\./,'').toLowerCase()}catch{return''}};
const blocked=v=>/(metacritic\.com|opencritic\.com|reddit\.com|steamcommunity\.com|youtube\.com|youtu\.be|fandom\.com|everand\.com|zencastr\.com|spotify\.com|podbean\.com|podcasts?\.)/i.test(host(v));
const bad=v=>/(walkthrough|guide|wiki|tips|cheat|news|preview|interview|podcast|episode|how[- ]?to|прохожд|гайд|новост|превью|интервью|подкаст)/i.test(String(v||''));
const tokens=title.toLowerCase().replace(/[^a-z0-9а-яё]+/gi,' ').trim().split(/\s+/).filter(t=>t.length>1||/^\d+$/.test(t));
const identity=v=>{const h=' '+String(v||'').toLowerCase().replace(/[^a-z0-9а-яё]+/gi,' ').replace(/\s+/g,' ').trim()+' ';return tokens.every(t=>h.includes(` ${t} `))};
const reviewish=v=>/(review|reviewed|retrospective|opinion|verdict|score|rating|реценз|обзор|ретро|мнение|вердикт)/i.test(String(v||''));
const sourceDomains=(config.sources||[]).filter(s=>s.enabled!==false&&s.family==='editorial').map(s=>{try{return{name:String(s.name||s.id),domain:new URL(s.url).hostname.replace(/^www\./,'')}}catch{return null}}).filter(Boolean);
for(const extra of [{name:'DTF',domain:'dtf.ru'},{name:'VGTimes',domain:'vgtimes.ru'},{name:'iXBT.games',domain:'ixbt.games'},{name:'GameMAG.ru',domain:'gamemag.ru'},{name:'Shazoo',domain:'shazoo.ru'},{name:'HonestGamers',domain:'honestgamers.com'},{name:'Cubed3',domain:'cubed3.com'}])if(!sourceDomains.some(x=>x.domain===extra.domain))sourceDomains.push(extra);
const knownPublication=url=>sourceDomains.find(s=>host(url)===s.domain||host(url).endsWith('.'+s.domain));

const headers={'user-agent':'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/127 Safari/537.36','accept-language':'en-US,en;q=.9,ru;q=.8'};
async function ddg(query){const u=new URL('https://html.duckduckgo.com/html/');u.searchParams.set('q',query);try{const r=await fetch(u,{headers,signal:AbortSignal.timeout(10000)});if(!r.ok)return{ok:false,items:[]};const html=await r.text(),items=[];for(const m of html.matchAll(/<a[^>]+class="[^"]*result__a[^"]*"[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/gi)){const url=canonical(m[1]);if(url)items.push({url,title:decodeHtml(m[2]),description:''})}return{ok:true,items}}catch{return{ok:false,items:[]}}}
async function ddgLite(query){const u=new URL('https://lite.duckduckgo.com/lite/');u.searchParams.set('q',query);try{const r=await fetch(u,{headers,signal:AbortSignal.timeout(10000)});if(!r.ok)return{ok:false,items:[]};const html=await r.text(),items=[];for(const m of html.matchAll(/<a[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/gi)){const url=canonical(m[1]),t=decodeHtml(m[2]);if(url&&/^https?:/.test(url)&&!host(url).includes('duckduckgo.com'))items.push({url,title:t,description:''})}return{ok:true,items}}catch{return{ok:false,items:[]}}}
async function bing(query){const u=new URL('https://www.bing.com/search');u.searchParams.set('format','rss');u.searchParams.set('count','50');u.searchParams.set('q',query);try{const r=await fetch(u,{headers,signal:AbortSignal.timeout(10000)});if(!r.ok)return{ok:false,items:[]};const xml=await r.text(),items=[];for(const m of xml.matchAll(/<item>([\s\S]*?)<\/item>/gi)){const b=m[1],pick=t=>decodeHtml((b.match(new RegExp(`<${t}>([\\s\\S]*?)<\\/${t}>`,'i'))||[])[1]||''),url=canonical(pick('link'));if(url)items.push({url,title:pick('title'),description:pick('description')})}return{ok:true,items}}catch{return{ok:false,items:[]}}}
async function google(query){const u=new URL('https://www.google.com/search');u.searchParams.set('q',query);u.searchParams.set('num','20');u.searchParams.set('filter','0');try{const r=await fetch(u,{headers,signal:AbortSignal.timeout(10000)});if(!r.ok)return{ok:false,items:[]};const html=await r.text(),items=[];for(const m of html.matchAll(/<a[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/gi)){const url=canonical(m[1]),t=decodeHtml(m[2]);if(url&&/^https?:/.test(url)&&!/(google\.|gstatic\.|googleusercontent\.)/i.test(host(url)))items.push({url,title:t,description:''})}return{ok:true,items}}catch{return{ok:false,items:[]}}}
async function yahoo(query){const u=new URL('https://search.yahoo.com/search');u.searchParams.set('p',query);try{const r=await fetch(u,{headers,signal:AbortSignal.timeout(10000)});if(!r.ok)return{ok:false,items:[]};const html=await r.text(),items=[];for(const m of html.matchAll(/<a[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/gi)){const url=canonical(m[1]),t=decodeHtml(m[2]);if(url&&/^https?:/.test(url)&&!host(url).includes('yahoo.com'))items.push({url,title:t,description:''})}return{ok:true,items}}catch{return{ok:false,items:[]}}}
async function search(query){const rs=await Promise.all([ddg(query),ddgLite(query),bing(query),google(query),yahoo(query)]),map=new Map();for(const r of rs)for(const x of r.items){const u=canonical(x.url);if(u&&!map.has(u))map.set(u,{...x,url:u})}return{providers:{duckduckgo:rs[0].ok,duckduckgo_lite:rs[1].ok,bing:rs[2].ok,google:rs[3].ok,yahoo:rs[4].ok},items:[...map.values()]}}

const querySet=new Set([`"${title}" review`,`"${title}" game review`,`"${title}" retrospective`,`"${title}" score review`,`"${title}" рецензия`,`"${title}" обзор`,year?`"${title}" review ${year}`:`"${title}" review`]);
for(const s of sourceDomains){querySet.add(`"${title}" review site:${s.domain}`);querySet.add(`"${title}" ${s.name} review`)}
for(const s of sourceDomains.filter(s=>/\.ru$/.test(s.domain)))querySet.add(`"${title}" обзор site:${s.domain}`);
const queries=[...querySet],candidates=new Map(),providerStats={duckduckgo:0,duckduckgo_lite:0,bing:0,google:0,yahoo:0,queries:queries.length};let cursor=0;
async function worker(){while(cursor<queries.length){const q=queries[cursor++],res=await search(q);for(const k of Object.keys(res.providers))if(res.providers[k])providerStats[k]++;for(const item of res.items){const url=canonical(item.url),text=`${item.title} ${item.description} ${url}`,known=knownPublication(url);if(!url||blocked(url)||bad(text)||!identity(text)||!reviewish(text))continue;if(!known&&!/(review|reviews|reviewed|recens|obzor|реценз|обзор)/i.test(new URL(url).pathname))continue;if(!candidates.has(url))candidates.set(url,{publication:known?.name||host(url),title:item.title||`${title} review`,url,source_kind:/retro|ретро/i.test(text)?'retrospective_review':'review',score:null,scale:null,grade:'',provenance:'independent-web-search'})}}}
await Promise.all(Array.from({length:Math.min(6,queries.length)},()=>worker()));

function extractScore(html,text){
  const pairs=[
    /"ratingValue"\s*:\s*"?([0-9]+(?:\.[0-9]+)?)"?[\s\S]{0,240}?"bestRating"\s*:\s*"?([0-9]+(?:\.[0-9]+)?)"?/i,
    /"bestRating"\s*:\s*"?([0-9]+(?:\.[0-9]+)?)"?[\s\S]{0,240}?"ratingValue"\s*:\s*"?([0-9]+(?:\.[0-9]+)?)"?/i
  ];
  for(let i=0;i<pairs.length;i++){const m=html.match(pairs[i]);if(m){const a=Number(i===0?m[1]:m[2]),b=Number(i===0?m[2]:m[1]);if(a>=0&&b>0&&a<=b)return{score:a,scale:b,grade:''}}}
  for(const rx of [/(?:overall\s+score|overall|score|rating|verdict|оценк\w*)\s*[:\-]?\s*([0-9]+(?:\.[0-9]+)?)\s*\/\s*(5|10|100)/i,/\b([0-9]+(?:\.[0-9]+)?)\s*\/\s*(10|100)\b/i]){const m=text.match(rx);if(m){const a=Number(m[1]),b=Number(m[2]);if(a>=0&&a<=b)return{score:a,scale:b,grade:''}}}
  const standalone=text.match(/(?:overall\s+score|final\s+score|review\s+score|score|rating|verdict|оценка)\s*[:\-]?\s*(100|[1-9][0-9](?:\.[0-9]+)?|10(?:\.0+)?|[0-9](?:\.[0-9]+)?)(?:\s|%|$)/i);
  if(standalone){const a=Number(standalone[1]);if(a>=0&&a<=100)return{score:a,scale:a>10?100:10,grade:''}}
  const g=text.match(/(?:grade|rating)\s*[:\-]?\s*([ABCDF][+-]?)(?:\s|<|$)/i);return{score:null,scale:null,grade:g?g[1].toUpperCase():''};
}
async function probe(raw){try{const r=await fetch(raw.url,{redirect:'follow',headers,signal:AbortSignal.timeout(10000)});if(!r.ok)return null;const type=(r.headers.get('content-type')||'').toLowerCase();if(!/html|text/.test(type))return null;const html=await r.text(),text=decodeHtml(html).slice(0,600000);if(!identity(`${raw.title} ${text.slice(0,15000)}`)||bad(`${raw.title} ${text.slice(0,1000)}`))return null;const found=extractScore(html,text);return{...raw,url:canonical(r.url||raw.url),score:found.score,scale:found.scale,grade:found.grade,score_eligible:Boolean((Number.isFinite(found.score)&&Number.isFinite(found.scale)&&found.scale>0)||found.grade),checked_at:checkedAt,validation:{status:'accepted-readable-link',http_status:r.status,checked_at:checkedAt,method:'independent-search-live-http-v2'}}}catch{return null}}
const raws=[...candidates.values()],accepted=[];let p=0;async function probeWorker(){while(p<raws.length){const live=await probe(raws[p++]);if(live)accepted.push(live)}}await Promise.all(Array.from({length:Math.min(10,raws.length||1)},()=>probeWorker()));
const unique=new Map();for(const x of accepted){const k=canonical(x.url).toLowerCase();if(!unique.has(k))unique.set(k,x)}const sources=[...unique.values()].sort((a,b)=>Number(b.score_eligible)-Number(a.score_eligible)||a.publication.localeCompare(b.publication,'en'));
const scored=sources.filter(x=>x.score_eligible).length,result={schema_version:2,game_slug:slug,title,checked_at:checkedAt,providers:providerStats,queries:queries.length,candidates:candidates.size,accepted:sources.length,scored,minimum_professional_sources:minimum,minimum_scored_sources:minimumScored,coverage_passed:sources.length>=minimum&&scored>=minimumScored,sources};
write(`data/research/${slug}-independent-web-sources.json`,result);write(`data/parser-runs/independent-web-sources-${slug}.json`,{parser:'independent-game-source-web-discovery',status:result.coverage_passed?'green':'needs_revision',checked_at:checkedAt,game_slug:slug,accepted:sources.length,scored,providers:providerStats,output:`data/research/${slug}-independent-web-sources.json`});console.log(JSON.stringify({...result,sources:undefined},null,2));
