#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';

const root=process.cwd();
const slug=String(process.argv[2]||'').trim();
if(!slug)throw new Error('Usage: node scripts/discover-game-sources-web.mjs <game-slug>');
const started=Date.now();
const read=(p,f=null)=>{try{return JSON.parse(fs.readFileSync(path.join(root,p),'utf8'))}catch{return f}};
const write=(p,v)=>{const t=path.join(root,p);fs.mkdirSync(path.dirname(t),{recursive:true});fs.writeFileSync(t,JSON.stringify(v,null,2)+'\n')};
const outputPath=`data/research/${slug}-independent-web-sources.json`;
const cached=read(outputPath,null);
if(cached&&process.env.IGROPOISK_SOURCE_DISCOVERY_REFRESH!=='1'){console.log(JSON.stringify({...cached,cached:true,sources:undefined},null,2));process.exit(0)}
const draft=read(`data/drafts/${slug}.json`);if(!draft?.identity?.title)throw new Error(`Missing draft for ${slug}`);
const config=read('config/parsers/review-synthesis.json',{}),quality=read('config/game-page-quality-v2.json',{}),policy=quality.game_source_corpus||{};
const title=String(draft.identity.title),year=Number(String(draft.release?.date||draft.release?.date_text||'').match(/(?:19|20)\d{2}/)?.[0]||0),minimum=Number(policy.minimum_professional_sources||10),minimumScored=Number(quality.rating?.minimum_sources||5),checkedAt=new Date().toISOString();
const decodeHtml=s=>String(s||'').replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#x27;|&#39;|&apos;/g,"'").replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/<[^>]*>/g,' ').replace(/\s+/g,' ').trim();
const canonical=v=>{try{const u=new URL(String(v||''),'https://duckduckgo.com');if(u.hostname.endsWith('duckduckgo.com')&&u.pathname.startsWith('/l/')){const t=u.searchParams.get('uddg');if(t)return canonical(decodeURIComponent(t))}if(/google\./i.test(u.hostname)&&u.pathname==='/url'){const t=u.searchParams.get('q')||u.searchParams.get('url');if(t)return canonical(t)}u.hash='';for(const k of ['utm_source','utm_medium','utm_campaign','utm_content','utm_term','ysclid'])u.searchParams.delete(k);return `${u.origin}${u.pathname.replace(/\/$/,'')}${u.search}`}catch{return''}};
const host=v=>{try{return new URL(v).hostname.replace(/^www\./,'').toLowerCase()}catch{return''}};
const technicalUrl=v=>{try{return/(^|\/)(?:_?captcha|login|signin|search)(?:[/?#]|$)/i.test(new URL(v).pathname)}catch{return true}};
const blocked=v=>/(metacritic\.com|opencritic\.com|steambase\.io|reddit\.com|steamcommunity\.com|youtube\.com|youtu\.be|fandom\.com|everand\.com|zencastr\.com|spotify\.com|podbean\.com|podcasts?\.)/i.test(host(v));
const bad=v=>/(walkthrough|guide|wiki|tips|cheat|news|preview|interview|podcast|episode|how[- ]?to|прохожд|гайд|новост|превью|интервью|подкаст)/i.test(String(v||''));
const tokens=title.toLowerCase().replace(/[^a-z0-9а-яё]+/gi,' ').trim().split(/\s+/).filter(t=>t.length>1||/^\d+$/.test(t));
const identity=v=>{const h=' '+String(v||'').toLowerCase().replace(/[^a-z0-9а-яё]+/gi,' ').replace(/\s+/g,' ').trim()+' ';return tokens.every(t=>h.includes(` ${t} `))};
const reviewish=v=>/(review|reviewed|retrospective|opinion|verdict|score|rating|article|реценз|обзор|ретро|мнение|вердикт)/i.test(String(v||''));
const sourceDomains=(config.sources||[]).filter(s=>s.enabled!==false&&s.family==='editorial').map(s=>{try{return{name:String(s.name||s.id),domain:new URL(s.url).hostname.replace(/^www\./,'')}}catch{return null}}).filter(Boolean);
for(const extra of [{name:'DTF',domain:'dtf.ru'},{name:'VGTimes',domain:'vgtimes.ru'},{name:'iXBT.games',domain:'ixbt.games'},{name:'GameMAG.ru',domain:'gamemag.ru'},{name:'Shazoo',domain:'shazoo.ru'},{name:'HonestGamers',domain:'honestgamers.com'},{name:'Cubed3',domain:'cubed3.com'},{name:'Gaming Nexus',domain:'gamingnexus.com'},{name:'Old-Games.ru',domain:'old-games.ru'},{name:'Absolute Games',domain:'ag.ru'},{name:'GameGuru',domain:'gameguru.ru'},{name:'Rock Paper Shotgun',domain:'rockpapershotgun.com'},{name:'RPGFan',domain:'rpgfan.com'}])if(!sourceDomains.some(x=>x.domain===extra.domain))sourceDomains.push(extra);
const knownPublication=url=>sourceDomains.find(s=>host(url)===s.domain||host(url).endsWith('.'+s.domain));
const headers={'user-agent':'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/127 Safari/537.36','accept-language':'en-US,en;q=.9,ru;q=.8'};
const timeout=ms=>AbortSignal.timeout(ms);
async function bing(query){const u=new URL('https://www.bing.com/search');u.searchParams.set('format','rss');u.searchParams.set('count','50');u.searchParams.set('q',query);try{const r=await fetch(u,{headers,signal:timeout(5000)});if(!r.ok)return{ok:false,items:[]};const xml=await r.text(),items=[];for(const m of xml.matchAll(/<item>([\s\S]*?)<\/item>/gi)){const b=m[1],pick=t=>decodeHtml((b.match(new RegExp(`<${t}>([\\s\\S]*?)<\\/${t}>`,'i'))||[])[1]||''),url=canonical(pick('link'));if(url)items.push({url,title:pick('title'),description:pick('description')})}return{ok:true,items}}catch{return{ok:false,items:[]}}}
async function google(query){const u=new URL('https://www.google.com/search');u.searchParams.set('q',query);u.searchParams.set('num','20');u.searchParams.set('filter','0');try{const r=await fetch(u,{headers,signal:timeout(5000)});if(!r.ok)return{ok:false,items:[]};const html=await r.text(),items=[];for(const m of html.matchAll(/<a[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/gi)){const url=canonical(m[1]),t=decodeHtml(m[2]);if(url&&/^https?:/.test(url)&&!/(google\.|gstatic\.|googleusercontent\.)/i.test(host(url)))items.push({url,title:t,description:''})}return{ok:true,items}}catch{return{ok:false,items:[]}}}
async function ddgLite(query){const u=new URL('https://lite.duckduckgo.com/lite/');u.searchParams.set('q',query);try{const r=await fetch(u,{headers,signal:timeout(5000)});if(!r.ok)return{ok:false,items:[]};const html=await r.text(),items=[];for(const m of html.matchAll(/<a[^>]+href="([^"]+)"[^>]*>([\s\S]*?)<\/a>/gi)){const url=canonical(m[1]),t=decodeHtml(m[2]);if(url&&/^https?:/.test(url)&&!host(url).includes('duckduckgo.com'))items.push({url,title:t,description:''})}return{ok:true,items}}catch{return{ok:false,items:[]}}}
async function search(query){const rs=await Promise.all([bing(query),google(query),ddgLite(query)]),map=new Map();for(const r of rs)for(const x of r.items){const u=canonical(x.url);if(u&&!map.has(u))map.set(u,{...x,url:u})}return{providers:{bing:rs[0].ok,google:rs[1].ok,duckduckgo_lite:rs[2].ok},items:[...map.values()]}}
const candidates=new Map();
function ingest(items,origin){for(const item of items){const url=canonical(item.url),text=`${item.title} ${item.description||''} ${url}`,known=knownPublication(url);if(!url||technicalUrl(url)||blocked(url)||bad(text)||!identity(text))continue;if(!known&&!reviewish(text)&&!/(review|reviews|reviewed|recens|obzor|article|реценз|обзор)/i.test(new URL(url).pathname))continue;if(!candidates.has(url))candidates.set(url,{publication:known?.name||host(url),title:item.title||`${title} review`,url,source_kind:/retro|ретро/i.test(text)?'retrospective_review':'review',score:null,scale:null,grade:'',provenance:origin})}}
const providerStats={bing:0,google:0,duckduckgo_lite:0,broad_queries:0,targeted_queries:0};
async function runQueries(queries,origin){const results=await Promise.all(queries.map(async q=>({q,res:await search(q)})));for(const {res} of results){for(const k of Object.keys(res.providers))if(res.providers[k])providerStats[k]++;ingest(res.items,origin)}return results.length}
const broad=[`"${title}" review`,`"${title}" reviews`,`"${title}" game review`,`"${title}" retrospective`,`"${title}" рецензия`,`"${title}" обзор`,year?`"${title}" review ${year}`:`"${title}" review`,`"${title}" рейтинг обзор`];
providerStats.broad_queries=await runQueries([...new Set(broad)],'broad-web-search');
function extractScore(html,text){const jsonPairs=[/"ratingValue"\s*:\s*"?([0-9]+(?:\.[0-9]+)?)"?[\s\S]{0,300}?"bestRating"\s*:\s*"?([0-9]+(?:\.[0-9]+)?)"?/i,/"bestRating"\s*:\s*"?([0-9]+(?:\.[0-9]+)?)"?[\s\S]{0,300}?"ratingValue"\s*:\s*"?([0-9]+(?:\.[0-9]+)?)"?/i];for(let i=0;i<jsonPairs.length;i++){const m=html.match(jsonPairs[i]);if(m){const score=Number(i===0?m[1]:m[2]),scale=Number(i===0?m[2]:m[1]);if(score>=0&&scale>0&&score<=scale)return{score,scale,grade:''}}}for(const rx of [/(?:overall\s+score|final\s+score|review\s+score|score|rating|verdict|оценк\w*)\s*[:\-]?\s*([0-9]+(?:\.[0-9]+)?)\s*\/\s*(5|10|20|100)/i,/\b([0-9]+(?:\.[0-9]+)?)\s*\/\s*(5|10|20|100)\b/i]){const m=text.match(rx);if(m){const score=Number(m[1]),scale=Number(m[2]);if(score>=0&&score<=scale)return{score,scale,grade:''}}}const pct=text.match(/(?:overall\s+score|final\s+score|review\s+score|score|rating|verdict|оценк\w*)\s*[:\-]?\s*([0-9]{1,3})\s*%/i);if(pct){const score=Number(pct[1]);if(score<=100)return{score,scale:100,grade:''}}const plain=text.match(/(?:overall\s+score|final\s+score|review\s+score|rating|verdict|оценка)\s*[:\-]?\s*([0-9]+(?:\.[0-9]+)?)(?!\s*\/)/i);if(plain){const score=Number(plain[1]);if(score>=0&&score<=100)return{score,scale:score>10?100:10,grade:''}}const g=text.match(/(?:grade|rating|verdict|оценка)\s*[:\-]?\s*([ABCDF][+-]?)(?:\s|<|$)/i);return{score:null,scale:null,grade:g?g[1].toUpperCase():''}}
async function probe(raw){try{const r=await fetch(raw.url,{redirect:'follow',headers,signal:timeout(7000)});if(!r.ok)return null;const finalUrl=canonical(r.url||raw.url);if(technicalUrl(finalUrl)||blocked(finalUrl))return null;const type=(r.headers.get('content-type')||'').toLowerCase();if(!/html|text/.test(type))return null;const html=await r.text(),text=decodeHtml(html).slice(0,700000);if(!identity(`${raw.title} ${text.slice(0,20000)}`))return null;const found=extractScore(html,text);const community=/^(?:dtf\.ru|stopgame\.ru|vgtimes\.ru|old-games\.ru|gameguru\.ru)$/i.test(host(finalUrl));const scoreEligible=!community&&Boolean((Number.isFinite(found.score)&&Number.isFinite(found.scale)&&found.scale>0)||found.grade);return{...raw,url:finalUrl,score:scoreEligible?found.score:null,scale:scoreEligible?found.scale:null,grade:scoreEligible?found.grade:'',score_eligible:scoreEligible,checked_at:checkedAt,validation:{status:'accepted-readable-link',http_status:r.status,checked_at:checkedAt,method:'broad-first-parallel-live-http-v5'}}}catch{return null}}
async function probeAll(raws){const out=[];let cursor=0;async function worker(){while(cursor<raws.length){const item=raws[cursor++],live=await probe(item);if(live)out.push(live)}}await Promise.all(Array.from({length:Math.min(24,raws.length||1)},()=>worker()));return out}
const publicationStats=list=>{const pubs=new Map();for(const x of list){const key=host(x.url)||String(x.publication||'').toLowerCase();const old=pubs.get(key);if(!old||(!old.score_eligible&&x.score_eligible))pubs.set(key,x)}return{count:pubs.size,scored:[...pubs.values()].filter(x=>x.score_eligible).length}};
let accepted=await probeAll([...candidates.values()]);let unique=new Map();for(const x of accepted){const k=canonical(x.url).toLowerCase();if(!unique.has(k))unique.set(k,x)}let sources=[...unique.values()],stats=publicationStats(sources);
if(stats.count<minimum||stats.scored<minimumScored){const represented=new Set(sources.map(x=>host(x.url)));const missing=sourceDomains.filter(s=>![...represented].some(h=>h===s.domain||h.endsWith('.'+s.domain)));const priority=['cubed3.com','gamingnexus.com','rpgfan.com','gamerevolution.com','ign.com','gamespot.com','eurogamer.net','igromania.ru','stopgame.ru','ag.ru','vgtimes.ru','gameguru.ru'];missing.sort((a,b)=>priority.indexOf(a.domain)===-1?1:priority.indexOf(b.domain)===-1?-1:priority.indexOf(a.domain)-priority.indexOf(b.domain));const targets=[];for(const s of missing){targets.push(`"${title}" site:${s.domain}`,`"${title}" review site:${s.domain}`);if(/\.ru$/.test(s.domain))targets.push(`"${title}" обзор site:${s.domain}`)}for(let i=0;i<targets.length&&(stats.count<minimum||stats.scored<minimumScored);i+=12){const batch=targets.slice(i,i+12);providerStats.targeted_queries+=await runQueries(batch,'targeted-publication-search');const known=new Set(sources.map(x=>canonical(x.url).toLowerCase()));const fresh=[...candidates.values()].filter(x=>!known.has(canonical(x.url).toLowerCase()));const extra=await probeAll(fresh);for(const x of extra){const k=canonical(x.url).toLowerCase();if(!unique.has(k))unique.set(k,x)}sources=[...unique.values()];stats=publicationStats(sources)}}
sources.sort((a,b)=>Number(b.score_eligible)-Number(a.score_eligible)||a.publication.localeCompare(b.publication,'en'));stats=publicationStats(sources);
const result={schema_version:5,game_slug:slug,title,checked_at:checkedAt,elapsed_ms:Date.now()-started,providers:providerStats,queries:providerStats.broad_queries+providerStats.targeted_queries,candidates:candidates.size,accepted:sources.length,publication_count:stats.count,scored:stats.scored,minimum_professional_sources:minimum,minimum_scored_sources:minimumScored,coverage_passed:stats.count>=minimum&&stats.scored>=minimumScored,sources};
write(outputPath,result);write(`data/parser-runs/independent-web-sources-${slug}.json`,{parser:'independent-game-source-web-discovery-v5',status:result.coverage_passed?'green':'needs_revision',checked_at:checkedAt,game_slug:slug,elapsed_ms:result.elapsed_ms,accepted:sources.length,publication_count:stats.count,scored:stats.scored,providers:providerStats,output:outputPath});console.log(JSON.stringify({...result,sources:undefined},null,2));
