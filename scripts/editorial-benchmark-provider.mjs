#!/usr/bin/env node
import crypto from 'node:crypto';
import {execFileSync} from 'node:child_process';

const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const first=(...names)=>names.map(n=>process.env[n]).find(v=>String(v||'').trim())||'';
async function fetchText(url,options={},timeout=240000){const c=new AbortController(),timer=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{...options,signal:c.signal}),text=await r.text();if(!r.ok){const e=new Error(`${r.status}: ${text.slice(0,1800)}`);e.status=r.status;e.headers=Object.fromEntries(r.headers.entries());throw e}return{text,headers:Object.fromEntries(r.headers.entries())}}finally{clearTimeout(timer)}}
async function retry(fn,n=5){let last;for(let i=0;i<n;i++){try{return await fn()}catch(e){last=e;const msg=String(e?.message||'');const budget402=Number(e.status)===402&&/in_flight_budget_exhausted|available credits given your current in-flight requests/i.test(msg);const transient=[408,409,425,429,500,502,503,504].includes(Number(e.status))||budget402||e?.name==='AbortError'||/fetch failed|timeout|ECONNRESET|socket/i.test(msg);if(!transient||i===n-1)break;const ra=Number(e.headers?.['retry-after']||0);const delay=budget402?Math.max(ra*1000,120000):Math.max(ra*1000,4000*(i+1));await sleep(delay)}}throw last}
function openAIContent(j){const c=j?.choices?.[0]?.message?.content;if(typeof c==='string')return c;if(Array.isArray(c))return c.map(x=>typeof x==='string'?x:(x?.text||'')).join('\n');return j?.output_text||''}
function curlJson(args){return JSON.parse(execFileSync('curl',args,{encoding:'utf8',maxBuffer:32*1024*1024,timeout:240000}))}

async function openrouter({model,system,prompt,maxTokens,temperature}){const key=first('OPENROUTER_API_KEY','OPEN_ROUTER_API_KEY');if(!key)throw Object.assign(new Error('OPENROUTER_API_KEY missing'),{code:'missing_secret'});async function request(route){const body={model:route,messages:[{role:'system',content:system},{role:'user',content:prompt}],max_tokens:maxTokens,temperature,top_p:.9,reasoning:{effort:'low',exclude:true}};const {text}=await retry(()=>fetchText('https://openrouter.ai/api/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json','HTTP-Referer':'https://nkuchenov-hash.github.io/Igropoisk/','X-Title':'Igropoisk editorial model qualification'},body:JSON.stringify(body)}));const j=JSON.parse(text),out=openAIContent(j);if(!String(out).trim())throw new Error('empty OpenRouter output');return{raw:out,usage:j.usage||null,route}}
  try{return await request(model)}catch(e){const msg=String(e?.message||'');const freeUnavailable=Number(e?.status)===404&&/paid version is available|use this slug instead/i.test(msg);const freeLimited=Number(e?.status)===429&&/free-models-per-day|temporarily rate-limited upstream|add your own api key|add 10 credits/i.test(msg);if(model.endsWith(':free')&&(freeUnavailable||freeLimited)){const paid=model.replace(/:free$/,'');const r=await request(paid);return{...r,route_fallback_from:model,route_fallback_reason:freeUnavailable?'free_route_unavailable_exact_paid_model':'free_route_rate_limited_exact_paid_model'}}throw e}}
async function groq({model,system,prompt,maxTokens,temperature}){const key=first('GROQ_API_KEY');if(!key)throw Object.assign(new Error('GROQ_API_KEY missing'),{code:'missing_secret'});const body={model,messages:[{role:'system',content:system},{role:'user',content:prompt}],max_completion_tokens:maxTokens,temperature,top_p:.9,reasoning_effort:model.includes('qwen')?'none':'low',reasoning_format:'hidden'};const {text}=await retry(()=>fetchText('https://api.groq.com/openai/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify(body)}));const j=JSON.parse(text),out=openAIContent(j);if(!String(out).trim())throw new Error('empty Groq output');return{raw:out,usage:j.usage||null,route:model}}
async function gemini({model,system,prompt,maxTokens,temperature}){const key=first('GEMINI_API_KEY','GOOGLE_AI_API_KEY','GOOGLE_GEMINI_API_KEY');if(!key)throw Object.assign(new Error('GEMINI_API_KEY missing'),{code:'missing_secret'});const body={systemInstruction:{parts:[{text:system}]},contents:[{role:'user',parts:[{text:prompt}]}],generationConfig:{maxOutputTokens:maxTokens,temperature,topP:.9}};const {text}=await retry(()=>fetchText(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:generateContent`,{method:'POST',headers:{'x-goog-api-key':key,'Content-Type':'application/json'},body:JSON.stringify(body)}));const j=JSON.parse(text),out=(j?.candidates?.[0]?.content?.parts||[]).map(x=>x?.text||'').join('\n');if(!String(out).trim())throw new Error(`empty Gemini output: ${text.slice(0,900)}`);return{raw:out,usage:j.usageMetadata||null,route:model}}
async function gigachat({model,system,prompt,maxTokens,temperature}){const credential=first('GIGACHAT_AUTH_KEY','GIGACHAT_CREDENTIALS','GIGACHAT_API_KEY','GIGACHAT_AUTHORIZATION_KEY','GIGACHAT_KEY');if(!credential)throw Object.assign(new Error('GigaChat authorization secret missing'),{code:'missing_secret'});const auth=/^Basic\s+/i.test(credential)?credential:`Basic ${credential}`,rq=crypto.randomUUID();const tok=curlJson(['-skL','-X','POST','https://ngw.devices.sberbank.ru:9443/api/v2/oauth','-H','Content-Type: application/x-www-form-urlencoded','-H','Accept: application/json','-H',`RqUID: ${rq}`,'-H',`Authorization: ${auth}`,'--data-urlencode','scope=GIGACHAT_API_PERS']);if(!tok?.access_token)throw new Error(`GigaChat OAuth failed: ${JSON.stringify(tok).slice(0,700)}`);const payload=JSON.stringify({model,messages:[{role:'system',content:system},{role:'user',content:prompt}],stream:false,temperature,top_p:.9,max_tokens:maxTokens});const j=curlJson(['-skL','-X','POST','https://api.giga.chat/v1/chat/completions','-H',`Authorization: Bearer ${tok.access_token}`,'-H','Content-Type: application/json','-H','Accept: application/json','--data-binary',payload]);if(j?.error||j?.status)throw new Error(`GigaChat error: ${JSON.stringify(j).slice(0,1200)}`);const out=openAIContent(j);if(!String(out).trim())throw new Error('empty GigaChat output');return{raw:out,usage:j.usage||null,route:model}}

export async function callEditorialModel({provider,model,system,prompt,maxTokens=1800,temperature=.55}){const fn=provider==='openrouter'?openrouter:provider==='groq'?groq:provider==='gemini'?gemini:provider==='gigachat'?gigachat:null;if(!fn)throw new Error(`unsupported provider ${provider}`);return fn({model,system,prompt,maxTokens,temperature})}
