#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';
import {generateFreeEditorialJSON,assertFreeEditorialAI} from './lib/free-editorial-ai.mjs';

const root=process.cwd(),slug=String(process.argv[2]||'').trim();if(!slug)throw new Error('Usage: node scripts/enrich-game-relations.mjs <slug>');
const read=(relative,fallback=null)=>{try{return JSON.parse(fs.readFileSync(path.join(root,relative),'utf8'))}catch{return fallback}};
const write=(relative,value)=>{const target=path.join(root,relative);fs.mkdirSync(path.dirname(target),{recursive:true});fs.writeFileSync(target,`${JSON.stringify(value,null,2)}\n`)};
const draft=read(`data/drafts/${slug}.json`),corpus=read(`data/game-sources/${slug}.json`,{});if(!draft?.identity?.title)throw new Error(`Missing data/drafts/${slug}.json`);
const checkedAt=new Date().toISOString(),title=draft.identity.title,currentYear=Number(String(draft.release?.date||draft.release?.date_text||'').match(/(?:19|20)\d{2}/)?.[0]||0);

// A finalized page package is immutable. Enrichment discovered later becomes a page revision task;
// only a draft/needs_revision package may be mutated before the canonical finalizer runs again.
if(draft.publication?.status==='published'&&draft.publication?.public_ready===true){
  const plan=read('data/content-pipeline/execution-plan.json',{schema_version:1,pages:[],reviews:[]});plan.pages=Array.isArray(plan.pages)?plan.pages:[];plan.reviews=Array.isArray(plan.reviews)?plan.reviews:[];
  if(!plan.pages.some(item=>item.slug===slug))plan.pages.push({type:'build_page',game_id:String(draft.game_id||draft.identity?.game_id||''),slug,title,steam_appid:Number(draft.identity?.steam_appid)||null,priority:850,reason:'relation_enrichment_requires_canonical_page_revision'});plan.updated_at=checkedAt;write('data/content-pipeline/execution-plan.json',plan);
  write(`data/parser-runs/relations-${slug}.json`,{parser:'game-relations-research',status:'queued',game_slug:slug,checked_at:checkedAt,public_package_preserved:true,publication_owner:'scripts/finalize-game-page-publication.mjs',comments:['Published canonical package is immutable; relation enrichment was queued as a full page revision.']});
  console.log(JSON.stringify({slug,status:'queued',published_package_preserved:true},null,2));process.exit(0);
}
let ai=null;
function revision(reason){write(`data/parser-runs/relations-${slug}.json`,{parser:'game-relations-research',status:'needs_revision',game_slug:slug,checked_at:checkedAt,provider:ai?.provider||'editorial-router',model:ai?.model||null,paid_api:false,paid_api_required:false,comments:[reason]});console.log(JSON.stringify({slug,status:'needs_revision',preserved_existing:true,reason},null,2));process.exit(0)}
try{ai=await assertFreeEditorialAI()}catch(error){revision(`Editorial provider router unavailable: ${error.message||error}. Existing relation data was preserved.`)}
const sourceFacts=(corpus?.sources||[]).map(s=>({name:s.publication||s.name||s.source_name||'',title:s.title||'',url:s.resolved_url||s.url||'',kind:s.kind||'',roles:s.roles||[]})).filter(s=>s.url);
const prompt=`Для Игропоиска структурируй связи и профиль сходства точной игры ${title} (${currentYear||'год уточняется'}). Используй только переданные данные. Не добавляй сведения по памяти модели. Верни только JSON с объектами franchise и similarity_profile.\n\nДанные игры:\n${JSON.stringify({identity:draft.identity,release:draft.release,classification:draft.classification,editorial:draft.editorial,companies:draft.companies,links:draft.links,existing_relations:draft.relations||null},null,2)}\n\nПроверенный корпус источников:\n${JSON.stringify(sourceFacts,null,2)}\n\nfranchise.name и franchise.games заполняй только если связь подтверждается переданными данными; иначе оставь пустыми. В games не включай DLC, soundtrack, demo и текущую игру. Для игры серии верни title, slug, release_year, steam_appid, relation, source_url; ничего не угадывай. franchise.evidence содержит только URL из переданного корпуса. similarity_profile должен содержать массивы genres, subgenres, gameplay_type, combat, perspective, world_structure, party_mode, narrative, progression, setting, tone, multiplayer, mechanics, semantic_tokens и описывать только свойства из classification/editorial.`;
let generated,relations;try{generated=await generateFreeEditorialJSON({system:'Ты редактор игровой базы данных. Не выдумывай факты. Возвращай только валидный JSON.',prompt,temperature:0.1});relations=generated.data;ai={...ai,provider:generated.provider,model:generated.model}}catch(error){revision(`Relation research through editorial router failed: ${error.message||error}. Existing data was preserved.`)}
const emptyProfile={genres:[],subgenres:[],gameplay_type:[],combat:[],perspective:[],world_structure:[],party_mode:[],narrative:[],progression:[],setting:[],tone:[],multiplayer:[],mechanics:[],semantic_tokens:[]};relations.franchise=relations?.franchise&&typeof relations.franchise==='object'?relations.franchise:{name:'',games:[],evidence:[]};relations.franchise.name=String(relations.franchise.name||'').trim();relations.franchise.games=(Array.isArray(relations.franchise.games)?relations.franchise.games:[]).filter(game=>game?.title&&String(game.title).toLowerCase()!==String(title).toLowerCase()).map(game=>({title:String(game.title||'').trim(),slug:String(game.slug||'').trim(),release_year:Number.isFinite(Number(game.release_year))?Number(game.release_year):null,steam_appid:Number.isFinite(Number(game.steam_appid))?Number(game.steam_appid):null,relation:String(game.relation||'').trim(),source_url:String(game.source_url||'').trim()})).filter(game=>game.slug&&game.source_url);relations.franchise.evidence=(Array.isArray(relations.franchise.evidence)?relations.franchise.evidence:[]).map(String).filter(url=>sourceFacts.some(source=>source.url===url));relations.similarity_profile={...emptyProfile,...(relations?.similarity_profile||{})};for(const key of Object.keys(emptyProfile))relations.similarity_profile[key]=(Array.isArray(relations.similarity_profile[key])?relations.similarity_profile[key]:[]).map(x=>String(x||'').trim()).filter(Boolean);
draft.relations={...(draft.relations||{}),...relations,checked_at:checkedAt,provider:ai.provider,model:ai.model,paid_api:false,paid_api_required:false};draft.publication={...(draft.publication||{}),status:'needs_revision',public_ready:false};write(`data/drafts/${slug}.json`,draft);write(`data/franchises/${slug}.json`,{schema_version:2,game_slug:slug,checked_at:checkedAt,status:relations.franchise.name&&relations.franchise.games.length?'green':'no-confirmed-franchise',provider:ai.provider,model:ai.model,paid_api:false,paid_api_required:false,...relations.franchise});write(`data/parser-runs/relations-${slug}.json`,{parser:'game-relations-research',status:'green',game_slug:slug,checked_at:checkedAt,provider:ai.provider,model:ai.model,paid_api:false,paid_api_required:false,franchise:relations.franchise.name||null,franchise_games:relations.franchise.games.length,comments:[]});
const queue=read('data/content-pipeline/franchise-queue.json',{schema_version:1,updated_at:checkedAt,items:[]}),existing=new Map((queue.items||[]).map(item=>[item.slug,item]));for(const game of relations.franchise.games){if(!game.slug||game.slug===slug)continue;existing.set(game.slug,{...(existing.get(game.slug)||{}),source_game:slug,title:game.title,slug:game.slug,release_year:game.release_year||null,steam_appid:game.steam_appid||null,source_url:game.source_url||'',status:existing.get(game.slug)?.status||'queued',updated_at:checkedAt})}queue.updated_at=checkedAt;queue.items=[...existing.values()];write('data/content-pipeline/franchise-queue.json',queue);console.log(JSON.stringify({slug,status:'green',provider:ai.provider,model:ai.model,paid_api:false,paid_api_required:false,franchise:relations.franchise.name||null,franchise_games:relations.franchise.games.length,queued:queue.items.filter(item=>item.status==='queued').length},null,2));
