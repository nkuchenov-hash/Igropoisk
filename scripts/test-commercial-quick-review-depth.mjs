#!/usr/bin/env node
import fs from 'node:fs';
const read=path=>fs.readFileSync(path,'utf8'),fail=message=>{throw new Error(message)};
const wrapper=read('scripts/build-review-bootstrap-commercial.mjs'),synthesis=read('scripts/build-review-bootstrap-commercial-local.mjs'),audit=read('scripts/audit-review-bootstrap-local.mjs'),githubModel=read('scripts/lib/github-editorial-model.mjs'),renderer=read('scripts/render-review-bootstrap.mjs'),workflow=read('.github/workflows/game-post-create-enrichment.yml');
for(const marker of ['build-review-bootstrap-commercial-local.mjs','audit-review-bootstrap-local.mjs','render-review-bootstrap.mjs','if(words<220)'])if(!wrapper.includes(marker))fail(`Commercial quick-review wrapper missing: ${marker}`);
if(wrapper.includes('build-review-bootstrap-local.mjs')||wrapper.includes('expand-review-bootstrap-local.mjs'))fail('Commercial wrapper still performs the slow draft-then-expand chain');
const auditAt=wrapper.indexOf('audit-review-bootstrap-local.mjs'),newRenderAt=wrapper.lastIndexOf('render-review-bootstrap.mjs');if(auditAt<0||newRenderAt<auditAt)fail('New commercial article can be rendered before factual/language audit passes');
for(const marker of ['280–380','EVIDENCE','words<220','paragraphs||[]).length!==2','minItems:4,maxItems:4','commercial_depth_gate:true','canonical_score_eligible','usedIds.size<3','githubChatJson','GITHUB_EDITORIAL_MODEL',"requestedProvider==='local'",'numCtx:5120','numPredict:1900','timeoutMs:300000'])if(!synthesis.includes(marker))fail(`Direct grounded commercial synthesis missing: ${marker}`);
for(const marker of ['citedIds','citedPublications','slice(0,5)','githubChatJson','GITHUB_AUDIT_MODEL',"requestedProvider==='local'",'quality_problems','!evidenceText.includes(value)','effectiveUnsupported','effectiveLanguage','effectiveQuality','missingReasons','discarded_advisory_issues',"article.publication_status=passed?'published':'needs_revision'"])if(!audit.includes(marker))fail(`Category-authoritative cited-source factual audit missing: ${marker}`);
if(audit.includes("required:['passed'"))fail('Audit still delegates the final passed verdict to a model');
for(const marker of ['https://models.github.ai/inference/chat/completions','response_format','timeoutMs=90000'])if(!githubModel.includes(marker))fail(`Fast GitHub Models client missing: ${marker}`);
for(const marker of ['data/review-bootstrap/${slug}.json',"path.join(root,'article',slug,'index.html')",'data/reviews/${slug}.json'])if(!renderer.includes(marker))fail(`Bootstrap renderer does not synchronize ${marker}`);
const pushStart=workflow.indexOf('  push:'),pullStart=workflow.indexOf('  pull_request:');if(pushStart<0||pullStart<pushStart)fail('Workflow trigger blocks missing');
const pushBlock=workflow.slice(pushStart,pullStart),pullBlock=workflow.slice(pullStart,workflow.indexOf('\npermissions:',pullStart));
for(const forbidden of ["data/game-enrichment-requests/*.json","game/*/index.html"])if(pushBlock.includes(forbidden))fail(`Request/page trigger still duplicates merged-PR execution through push: ${forbidden}`);
for(const required of ["data/game-enrichment-requests/*.json","game/*/index.html"])if(!pullBlock.includes(required))fail(`Merged-PR trigger lost required path: ${required}`);
for(const marker of ['models: read','GITHUB_REVIEW_MODEL: openai/gpt-4.1','GITHUB_AUDIT_MODEL: openai/gpt-4.1','EDITORIAL_PROVIDER: github','scripts/lib/github-editorial-model.mjs','for attempt in 1 2; do','Grounded quick-review generation pass $attempt/2'])if(!workflow.includes(marker))fail(`Commercial fast-primary workflow wiring missing: ${marker}`);
console.log('Commercial quick-review contract passed: GPT-4.1 primary grounded 4x2 synthesis plus code-derived factual/language audit, local qwen fallback retained, >=220 words, audit-before-render and one merged-PR trigger.');
