import fs from 'node:fs';
import path from 'node:path';

const root=process.cwd();
const slug=process.argv[2];
if(!slug) throw new Error('Usage: node scripts/discover-game-videos.mjs <game-slug>');
const read=(file,fallback=null)=>{try{return JSON.parse(fs.readFileSync(path.join(root,file),'utf8'))}catch{return fallback}};
const write=(file,value)=>{const target=path.join(root,file);fs.mkdirSync(path.dirname(target),{recursive:true});fs.writeFileSync(target,JSON.stringify(value,null,2)+'\n')};
const draftPath=`data/drafts/${slug}.json`;
const draft=read(draftPath);
if(!draft) throw new Error(`Missing ${draftPath}`);
const checkedAt=new Date().toISOString();
const title=String(draft.identity?.title||slug).trim();
const year=String(draft.release?.canonical_date_text||draft.release?.date_text||draft.release?.date||'').match(/(?:19|20)\d{2}/)?.[0]||'';
const normalize=value=>String(value||'').normalize('NFKD').toLowerCase().replace(/[^a-z0-9а-яё]+/gi,' ').replace(/\s+/g,' ').trim();
const titleTokens=normalize(title).split(' ').filter(Boolean);
const identityOk=value=>{const hay=` ${normalize(value)} `;return titleTokens.every(token=>hay.includes(` ${token} `))};
const canonical=value=>{try{const u=new URL(value);u.hash='';for(const key of ['utm_source','utm_medium','utm_campaign','utm_content','utm_term','si'])u.searchParams.delete(key);return `${u.origin}${u.pathname}${u.search}`}catch{return String(value||'').trim()}};
const host=value=>{try{return new URL(value).hostname.replace(/^www\./,'').toLowerCase()}catch{return''}};
const decode=v=>String(v||'').replace(/<!\[CDATA\[|\]\]>/g,'').replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#39;|&apos;/g,"'").replace(/&lt;/g,'<').replace(/&gt;/g,'>').replace(/\\u0026/g,'&').replace(/\\u003d/g,'=').replace(/\\\//g,'/').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim();
const decodeJsonText=value=>{try{return JSON.parse(`"${String(value||'').replace(/"/g,'\\"')}"`)}catch{return String(value||'').replace(/\\u0026/g,'&').replace(/\\u003d/g,'=').replace(/\\"/g,'"').replace(/\\n/g,' ')}};
const classify=text=>{const v=normalize(text);if(/review|обзор|ретро обзор|разбор|verdict|retrospective|retroview/.test(v))return'review';if(/gameplay|геймплей|walkthrough|playthrough/.test(v))return'gameplay';if(/interview|интервью|developer diary|dev diary|behind the scenes/.test(v))return'interview';if(/trailer|трейлер|teaser|тизер|cinematic|launch/.test(v))return'trailer';return'other'};
const videoHosts=new Set(['youtube.com','youtu.be','vk.com','vkvideo.ru','rutube.ru']);
const directVideoUrl=url=>{try{const u=new URL(url);const h=host(url);if(!videoHosts.has(h)&&![...videoHosts].some(x=>h.endsWith(`.${x}`)))return false;if(h==='youtube.com'||h.endsWith('.youtube.com'))return (u.pathname==='/watch'&&Boolean(u.searchParams.get('v')))||u.pathname.startsWith('/shorts/');if(h==='youtu.be')return u.pathname.split('/').filter(Boolean).length===1;if(h==='vk.com'||h==='vkvideo.ru')return /video/i.test(u.pathname+u.search);if(h==='rutube.ru')return /\/video\//i.test(u.pathname);return false}catch{return false}};
async function live(url){return{ok:directVideoUrl(url),url,status:null,method:'structural-direct-video-url'}}
const parseRss=xml=>{const out=[];for(const m of String(xml||'').matchAll(/<item>([\s\S]*?)<\/item>/gi)){const body=m[1],pick=tag=>decode((body.match(new RegExp(`<${tag}>([\\s\\S]*?)<\\/${tag}>`,'i'))||[])[1]||'');const url=pick('link'),name=pick('title'),description=pick('description');if(url)out.push({url,title:name,description})}return out};
async function bingRss(query){const u=new URL('https://www.bing.com/search');u.searchParams.set('format','rss');u.searchParams.set('count','50');u.searchParams.set('q',query);try{const r=await fetch(u,{redirect:'follow',signal:AbortSignal.timeout(12000),headers:{'user-agent':'Mozilla/5.0 (compatible; IgropoiskVideoDiscovery/4.0)','accept-language':'en-US,en;q=.9,ru;q=.8'}});if(!r.ok)return[];return parseRss(await r.text())}catch{return[]}}
function youtubeLinksFromHtml(html){const decoded=decode(html),out=new Set();for(const source of [html,decoded]){for(const m of source.matchAll(/https?:\\?\/\\?\/(?:www\.)?youtube\.com\/watch\?v=[A-Za-z0-9_-]{6,}(?:[^"'<>\\\s]*)?/gi)){let value=m[0].replace(/\\\//g,'/').replace(/&amp;/g,'&');try{const u=new URL(value);const v=u.searchParams.get('v');if(v)out.add(`https://www.youtube.com/watch?v=${v}`)}catch{}}for(const m of source.matchAll(/https?:\\?\/\\?\/youtu\.be\/[A-Za-z0-9_-]{6,}/gi))out.add(m[0].replace(/\\\//g,'/'))}return [...out]}
async function bingHtml(query){const u=new URL('https://www.bing.com/search');u.searchParams.set('count','50');u.searchParams.set('q',query);try{const r=await fetch(u,{redirect:'follow',signal:AbortSignal.timeout(15000),headers:{'user-agent':'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/127 Safari/537.36','accept-language':'en-US,en;q=.9,ru;q=.8'}});if(!r.ok)return[];return youtubeLinksFromHtml(await r.text()).map(url=>({url,title:query.replace(/site:[^ ]+/g,'').replace(/["']/g,'').trim(),description:query}))}catch{return[]}}
async function youtubeSearch(query,queryKind){const u=new URL('https://www.youtube.com/results');u.searchParams.set('search_query',query);try{const r=await fetch(u,{redirect:'follow',signal:AbortSignal.timeout(18000),headers:{'user-agent':'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/127 Safari/537.36','accept-language':'en-US,en;q=.9,ru;q=.8','cookie':'CONSENT=YES+cb.20210328-17-p0.en+FX+917'}});if(!r.ok)return[];const html=await r.text(),out=[],seen=new Set();for(const match of html.matchAll(/"videoRenderer"\s*:\s*\{/g)){const start=match.index||0,chunk=html.slice(start,start+9000);const id=(chunk.match(/"videoId"\s*:\s*"([A-Za-z0-9_-]{11})"/)||[])[1];if(!id||seen.has(id))continue;const rawTitle=(chunk.match(/"title"\s*:\s*\{\s*"runs"\s*:\s*\[\s*\{\s*"text"\s*:\s*"((?:\\.|[^"\\])*)"/)||[])[1]||'';const videoTitle=decodeJsonText(rawTitle);if(!videoTitle||!identityOk(videoTitle))continue;seen.add(id);out.push({url:`https://www.youtube.com/watch?v=${id}`,title:videoTitle,description:query,source_name:'YouTube',source_url:`https://www.youtube.com/watch?v=${id}`,kind:queryKind==='review'?'review':classify(`${videoTitle} ${query}`),published_at:''});if(out.length>=20)break}return out}catch{return[]}}
const existing=Array.isArray(draft.media?.videos)?draft.media.videos:[];
let discovered=[];
if(process.env.OPENAI_API_KEY){
 const schema={type:'object',additionalProperties:false,required:['videos'],properties:{videos:{type:'array',items:{type:'object',additionalProperties:false,required:['title','url','source_name','source_url','kind','published_at'],properties:{title:{type:'string'},url:{type:'string'},source_name:{type:'string'},source_url:{type:'string'},kind:{type:'string',enum:['trailer','gameplay','review','interview','other']},published_at:{type:'string'}}}}}};
 const prompt=`Найди максимально полный набор ПРЯМЫХ видеоссылок именно об игре ${title}${year?` (${year})`:''}. Ищи официальные трейлеры, gameplay, профессиональные видеообзоры и ретрообзоры, интервью и дневники разработчиков. Обязательно ищи YouTube, VK Video и Rutube. Для YouTube возвращай только URL вида https://www.youtube.com/watch?v=VIDEO_ID или youtu.be/VIDEO_ID, не страницы поиска, не Steam и не статьи. Приоритет: несколько независимых видеообзоров/ретрообзоров. Не включай материалы о другой части серии, ремейках, модах или экранизациях. Для каждого результата нужен прямой URL видео, точное название ролика, канал/издание и URL страницы-источника.`;
 try{const response=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{authorization:`Bearer ${process.env.OPENAI_API_KEY}`,'content-type':'application/json'},body:JSON.stringify({model:process.env.OPENAI_RESEARCH_MODEL||process.env.OPENAI_MODEL||'gpt-5',tools:[{type:'web_search',search_context_size:'high'}],tool_choice:'required',input:prompt,text:{format:{type:'json_schema',name:'igropoisk_video_discovery',strict:true,schema}}})});if(response.ok){const data=await response.json();const text=data.output_text||data.output?.flatMap(x=>x.content||[]).find(x=>x.type==='output_text')?.text;if(text){try{discovered=JSON.parse(text).videos||[]}catch{}}}}catch{}
}
const searchQueries=[
 {query:`${title} retrospective review`,kind:'review'},
 {query:`${title} review`,kind:'review'},
 {query:`${title} retro review`,kind:'review'},
 {query:`${title} обзор`,kind:'review'},
 {query:`${title} ретро обзор`,kind:'review'},
 {query:`${title} gameplay`,kind:'gameplay'},
 {query:`${title} trailer`,kind:'trailer'}
];
for(const spec of searchQueries){const generic=`"${title}" ${spec.query.replace(title,'').trim()} youtube`;const results=[...(await youtubeSearch(spec.query,spec.kind)),...(await bingRss(generic)),...(await bingHtml(generic))];for(const item of results){if(!directVideoUrl(item.url))continue;discovered.push({title:item.title,url:item.url,description:item.description||'',source_name:item.source_name||host(item.url),source_url:item.source_url||item.url,kind:item.kind&&item.kind!=='other'?item.kind:(spec.kind||classify(`${item.title} ${item.description}`)),published_at:item.published_at||''})}}
for(const query of [`"${title}" обзор rutube`,`"${title}" обзор vk video`]){for(const item of await bingRss(query)){if(!directVideoUrl(item.url))continue;discovered.push({title:item.title,url:item.url,description:item.description||'',source_name:host(item.url),source_url:item.url,kind:classify(`${item.title} ${item.description}`),published_at:''})}}
const merged=[...existing,...discovered],accepted=[],rejected=[],seen=new Set();
for(const raw of merged){const url=canonical(raw.url);const titleText=String(raw.title||'').trim();const reasons=[];if(!url)reasons.push('missing direct video URL');if(url&&!directVideoUrl(url))reasons.push('not a supported direct video URL');if(titleText&&!identityOk(`${titleText} ${raw.description||''} ${url}`))reasons.push('game identity mismatch');if(!titleText)reasons.push('missing title');const key=url.toLowerCase();if(key&&seen.has(key))reasons.push('duplicate URL');let verified=null;if(!reasons.length){verified=await live(url);if(!verified.ok)reasons.push('invalid direct video URL')}if(reasons.length){rejected.push({title:titleText,url,reasons});continue}seen.add(key);accepted.push({title:titleText,url:verified.url||url,thumbnail:String(raw.thumbnail||''),source_name:String(raw.source_name||raw.channel||host(url)),source_url:canonical(raw.source_url||url),kind:['trailer','gameplay','review','interview','other'].includes(raw.kind)?raw.kind:classify(titleText),published_at:String(raw.published_at||''),validation:{status:'accepted-video-link',checked_at:checkedAt,method:verified.method,http_status:verified.status}})}
draft.media=draft.media||{};draft.media.videos=accepted;write(draftPath,draft);const counts={accepted:accepted.length,rejected:rejected.length,video_reviews:accepted.filter(x=>x.kind==='review').length,trailers:accepted.filter(x=>x.kind==='trailer').length,gameplay:accepted.filter(x=>x.kind==='gameplay').length};write(`data/video-candidates/${slug}.json`,{schema_version:4,game_slug:slug,checked_at:checkedAt,collect_all_discovered:true,provider_independent_search:true,search_methods:['openai-web-search-optional','youtube-search-html','bing-rss','bing-html-direct-url-extraction'],queries:searchQueries.map(x=>x.query),accepted,rejected,counts});write(`data/parser-runs/video-discovery-${slug}.json`,{parser:'game-video-discovery-v4',status:accepted.length?'completed':'needs_revision',game_slug:slug,checked_at:checkedAt,...counts,collect_all_discovered:true,provider_independent_search:true});console.log(JSON.stringify({slug,...counts,provider_independent_search:true},null,2));if(!accepted.length)process.exitCode=2;
