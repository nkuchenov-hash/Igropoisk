#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
import {callEditorialModel} from './editorial-benchmark-provider.mjs';

const root=process.cwd(),slug=String(process.env.BENCH_GAME||'').trim(),provider=String(process.env.BENCH_PROVIDER||'').trim(),model=String(process.env.BENCH_MODEL||'').trim(),id=String(process.env.BENCH_ID||'').trim(),label=String(process.env.BENCH_LABEL||model).trim(),titleOverride=String(process.env.BENCH_TITLE||'').trim(),year=String(process.env.BENCH_YEAR||'').trim();if(!slug||!provider||!model||!id)throw new Error('BENCH_GAME/BENCH_PROVIDER/BENCH_MODEL/BENCH_ID required');const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const evidenceDir=path.join(root,'benchmark-evidence',slug),contentPath=path.join(evidenceDir,'source-content.json'),raw=fs.readFileSync(contentPath),sourceDoc=JSON.parse(raw.toString('utf8')),evidenceHash=crypto.createHash('sha256').update(raw).digest('hex'),title=titleOverride||sourceDoc.title||slug,readable=(sourceDoc.sources||[]).filter(s=>s?.readable===true&&String(s.text||'').trim().length>0);if(!readable.length)throw new Error(`No readable sources for ${slug}`);
const blocks=readable.map((s,i)=>`[SOURCE ${i+1}/${readable.length}]\nID: ${s.id||`source-${i+1}`}\nNAME: ${s.name||''}\nTITLE: ${s.title||''}\nURL: ${s.resolved_url||s.url||''}\nTEXT:\n${String(s.text||'').trim()}\n[/SOURCE]`),evidenceChars=blocks.reduce((n,x)=>n+x.length,0),CHUNK=provider==='groq'?12000:44000;function chunksOf(xs){const out=[];let cur='';for(const b of xs){if(b.length>CHUNK){if(cur){out.push(cur);cur=''}for(let p=0;p<b.length;p+=CHUNK)out.push(b.slice(p,p+CHUNK));continue}if(cur&&cur.length+b.length+2>CHUNK){out.push(cur);cur=''}cur+=(cur?'\n\n':'')+b}if(cur)out.push(cur);return out}const chunks=chunksOf(blocks);
const SYSTEM=`Ты — ведущий автор Игропоиска. Пишешь журнальный evergreen-обзор конкретной игры только по переданному каноническому корпусу. Не используй знания из памяти. Не смешивай оригинал с ремейками, ремастерами, портами, сиквелами и другими версиями. Не упоминай источники, ИИ, benchmark или процесс. Механики описывай в настоящем времени. Стиль: живой, конкретный, взрослый редакционный русский без рекламного пафоса и нейросетевых клише.`;
async function evidencePass(chunk,index,total){const prompt=`Игра: ${title}${year?` (${year})`:''}.\n\nEvidence pass ${index+1}/${total}. Извлеки из этой части корпуса материал для будущего большого обзора. Нужны подтверждённые: механики, структура, сюжетная роль без избыточного пересказа, интерфейс/HUD/сохранения/управление, сильные стороны, реальные слабости, необычные детали, атмосфера только когда она подтверждается конкретикой, исторический контекст только если объясняет дизайн. Отмечай противоречия/сомнительные детали. Не пиши обзор сейчас и ничего не добавляй из памяти. Сжимай повторяющиеся факты, но не выбрасывай характерные детали.\n\nCORPUS CHUNK:\n${chunk}`;const r=await callEditorialModel({provider,model,system:SYSTEM,prompt,maxTokens:1800,temperature:.2});return r.raw}
function reviewPrompt(notes){return `Игра: ${title}${year?` (${year})`:''}.\n\nНа основе evidence notes, которые ТЫ ЖЕ извлёк из 100% readable-корпуса, напиши полноценный обзор Игропоиска.\n\nЖЁСТКИЕ ТРЕБОВАНИЯ:\n- evergreen;\n- итоговый текст 1700–2800 русских слов, целевой объём около 2100–2300;\n- строго 7–9 содержательных разделов, никогда не больше 9;\n- чтобы выдержать объём, большинство разделов должны быть примерно по 220–320 слов;\n- заголовки называют конкретный аспект самой игры, а не шаблонные «Геймплей/Графика/Итоги»;\n- конкретные механики и детали важнее общих оценок;\n- достоинства и слабости вплетай в соответствующие системы;\n- проверь интерфейс/HUD/карту/здоровье/инвентарь/сохранения/управление/звук только там, где это подтверждено;\n- не выдумывай атмосферные детали, цифры, миссии, предметы и причинно-следственные связи;\n- не превращай обзор в пересказ сюжета;\n- финальный раздел должен формулировать характер/послевкусие игры, а не повторять предыдущие разделы;\n- запрещены «по современным меркам», «сегодня ощущается», «выдаёт свой возраст», «состарилось плохо», «для современного игрока»;\n- не упоминай evidence, источники, модель или процесс.\n\nПеред ответом молча проверь: все ли существенные утверждения опираются на notes, нет ли лишних выдуманных деталей, 7–9 ли разделов и достигнут ли минимум 1700 слов.\n\nФормат:\n<<<REVIEW>>>\n# [заголовок]\n\n## [раздел]\n...\n<<<END>>>\n\nEVIDENCE NOTES:\n${notes}`}
function section(rawText){return(String(rawText||'').match(/<<<REVIEW>>>\s*([\s\S]*?)(?=<<<END>>>|$)/i)?.[1]||'').trim()}function countWords(s){return String(s||'').trim().split(/\s+/u).filter(Boolean).length}function countSections(s){return(String(s||'').match(/^##\s+/gm)||[]).length}
const started=Date.now();let result={schema_version:3,test:'review-model-qualification',qualification:true,game_slug:slug,game_title:title,year,id,label,provider,model,evidence_sha256:evidenceHash,total_candidates:Number(sourceDoc.total_candidates||sourceDoc.sources?.length||0),readable_sources:readable.length,evidence_chars:evidenceChars,chunk_size_chars:CHUNK,chunk_count:chunks.length,coverage_source_ids:readable.map((s,i)=>s.id||`source-${i+1}`),status:'error',review:'',review_words:0,section_count:0,format_ok:false,structural_contract_ok:false,error:null,route_used:null,elapsed_ms:0};
try{const notes=[];for(let i=0;i<chunks.length;i++){notes.push(`=== CHUNK ${i+1}/${chunks.length} NOTES ===\n${await evidencePass(chunks[i],i,chunks.length)}`);if(provider==='groq')await sleep(30000)}const joined=notes.join('\n\n'),outDir=path.join(root,'benchmark-review-one');fs.mkdirSync(outDir,{recursive:true});fs.writeFileSync(path.join(outDir,'evidence-notes.txt'),joined);const response=await callEditorialModel({provider,model,system:SYSTEM,prompt:reviewPrompt(joined),maxTokens:7800,temperature:.56});result.route_used=response.route||model;const review=section(response.raw);result.review=review;result.format_ok=Boolean(review&&/<<<END>>>/i.test(response.raw));result.status=review?'ok':'partial';if(!review)result.error='REVIEW section missing'}catch(e){result.status=e?.code==='missing_secret'?'unavailable':'error';result.error=String(e?.message||e).slice(0,4000)}result.review_words=countWords(result.review);result.section_count=countSections(result.review);result.structural_contract_ok=result.status==='ok'&&result.format_ok&&result.review_words>=1600&&result.review_words<=3200&&result.section_count>=7&&result.section_count<=9;result.elapsed_ms=Date.now()-started;result.completed_at=new Date().toISOString();const out=path.join(root,'benchmark-review-one');fs.mkdirSync(out,{recursive:true});fs.writeFileSync(path.join(out,'result.json'),JSON.stringify(result,null,2)+'\n');fs.writeFileSync(path.join(out,'output.md'),`GAME: ${title}\nMODEL: ${label}\nSTATUS: ${result.status}\nROUTE: ${result.route_used||model}\nEVIDENCE_SHA256: ${evidenceHash}\nREADABLE: ${readable.length}\nCHUNKS: ${chunks.length}\nWORDS: ${result.review_words}\nSECTIONS: ${result.section_count}\nSTRUCTURAL_CONTRACT: ${result.structural_contract_ok}\n\n${result.review}\n\nERROR: ${result.error||''}\n`);console.log(JSON.stringify({game:slug,model:label,status:result.status,route:result.route_used,words:result.review_words,sections:result.section_count,contract:result.structural_contract_ok,chunks:chunks.length,elapsed_ms:result.elapsed_ms,error:result.error},null,2));
