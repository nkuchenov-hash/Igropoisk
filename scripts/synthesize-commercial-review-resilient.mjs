#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';
import {spawnSync} from 'node:child_process';
import {chatJson,localModelReady,LOCAL_EDITORIAL_MODEL} from './lib/local-editorial-model.mjs';

const root=process.cwd(),slug=String(process.argv[2]||'').trim().toLowerCase();if(!slug)throw new Error('Usage: synthesize-commercial-review-resilient <slug>');
if(process.env.OPENAI_API_KEY){const legacy=spawnSync('node',['scripts/synthesize-commercial-review-openai.mjs',slug],{cwd:root,encoding:'utf8',stdio:'pipe',env:process.env,maxBuffer:48*1024*1024});if(legacy.status===0){if(legacy.stdout)process.stdout.write(legacy.stdout);process.exit(0)}console.error(`[${slug}] OpenAI synthesis unavailable, switching to local fallback: ${String(legacy.stderr||legacy.stdout||'').slice(-2000)}`)}
const read=(r,f=null)=>{try{return JSON.parse(fs.readFileSync(path.join(root,r),'utf8'))}catch{return f}},write=(r,v)=>{const t=path.join(root,r);fs.mkdirSync(path.dirname(t),{recursive:true});fs.writeFileSync(t,`${JSON.stringify(v,null,2)}\n`)};
const contract=read('config/review-commercial-contract.json',{}),rules=contract.article||{},sourceRules=contract.source_corpus||{},game=read(`data/drafts/${slug}.json`),corpus=read(`data/review-article-corpus/${slug}.json`),reviews=read(`data/reviews/${slug}.json`,{}),ratings=read(`data/ratings/${slug}.json`,{}),discovery=read(`data/review-discovery-audits/${slug}.json`,{});if(!game?.identity?.title||!corpus?.coverage?.passed)throw new Error(`${slug}: canonical game/article corpus missing`);if(!await localModelReady({timeoutMs:5000}))throw new Error(`${slug}: neither OpenAI nor local ${LOCAL_EDITORIAL_MODEL} is available`);
const sources=(corpus.sources||[]).filter(x=>x?.source_role==='professional_review'||!x?.source_role),preferred=Math.max(8,Number(sourceRules.preferred_minimum_independent_full_reviews||15)),exhaustive=corpus.coverage?.exhaustive_discovery===true&&discovery?.exhaustive===true;if(!sources.length)throw new Error(`${slug}: no professional full-text review dossiers`);if(sources.length<preferred&&!exhaustive)throw new Error(`${slug}: source corpus below preferred target without exhaustive discovery proof`);
const minWords=Math.max(2800,Number(rules.minimum_words||3000)),targetWords=Math.max(minWords,Number(rules.target_words||3400)),maxWords=Math.max(targetWords,Number(rules.maximum_words_without_editor_approval||4500)),minSections=Math.max(7,Number(rules.minimum_sections||8)),targetSections=Math.max(minSections,Number(rules.target_sections||9)),maxSections=Math.max(targetSections,Number(rules.maximum_sections||10)),minParagraphs=Math.max(3,Number(rules.minimum_paragraphs_per_section||3)),minSectionWords=Math.max(220,Number(rules.minimum_words_per_section||260)),minSectionSources=sources.length>=preferred?Math.max(2,Number(rules.preferred_minimum_sources_per_section||3)):Math.max(1,Number(rules.minimum_sources_per_section_after_exhaustive_discovery||1)),minUsed=sources.length>=preferred?Math.min(sources.length,Math.max(10,Number(rules.preferred_minimum_materially_used_sources||12))):sources.length;
const countWords=v=>(String(v||'').match(/[A-Za-zА-Яа-яЁё0-9’'-]+/g)||[]).length,lowerLatin=v=>[...String(v||'').matchAll(/\b[a-z][a-z-]{2,}\b/g)].map(x=>x[0]).filter(x=>!['fallout','rpg'].includes(x));
const sourceDigest=sources.map(s=>({id:s.id,publication:s.publication,title:s.title,body_words:s.body_words,dossier:s.dossier})),identity={title:game.identity.title,series:game.identity.series||null,release:game.release,developers:game.companies?.developers||[],publishers:game.companies?.publishers||[],genres:game.classification?.genres||[],platforms:game.classification?.platforms||[],description:game.editorial?.integrated_description||game.editorial?.short_description||''};
const schema={type:'object',additionalProperties:false,required:['title','dek','lead','sections','verdict','used_source_ids'],properties:{title:{type:'string'},dek:{type:'string'},lead:{type:'string'},sections:{type:'array',minItems:minSections,maxItems:maxSections,items:{type:'object',additionalProperties:false,required:['id','heading','paragraphs','source_ids','image_caption'],properties:{id:{type:'string'},heading:{type:'string'},paragraphs:{type:'array',minItems:minParagraphs,maxItems:6,items:{type:'string'}},source_ids:{type:'array',minItems:minSectionSources,items:{type:'string'}},image_caption:{type:'string'}}}},verdict:{type:'object',additionalProperties:false,required:['summary','best_for','not_for'],properties:{summary:{type:'string'},best_for:{type:'array',items:{type:'string'}},not_for:{type:'array',items:{type:'string'}}}},used_source_ids:{type:'array',items:{type:'string'}}}};
const corpusNote=sources.length>=preferred?`Корпус широкий: ${sources.length} полнотекстовых независимых рецензий; используй минимум ${minUsed}.`:`После документированного исчерпывающего поиска реально найдено ${sources.length} полнотекстовых профессиональных рецензий. Используй ВСЕ ${sources.length} глубоко и не придумывай отсутствующие мнения.`;
const basePrompt=`Напиши большой журнальный обзор Игропоиска игры ${identity.title}. ${corpusNote}\nЖЁСТКИЕ ПРАВИЛА:\n- ${minSections}–${maxSections} смысловых разделов, цель ${targetSections}; минимум ${minParagraphs} полноценных абзаца в каждом.\n- Не меньше ${minWords} содержательных слов, цель ${targetWords}, максимум ${maxWords}; без повторов и искусственного растягивания.\n- Каждый раздел раскрывает конкретную сторону именно этой игры и имеет минимум ${minSectionSources} source_ids из предоставленных досье.\n- По статье в целом используй минимум ${minUsed} разных источников.\n- Не выдумывай факты, числа, механику, события или оценки. Если досье не подтверждает деталь — не пиши её.\n- Живой современный русский язык без кальки, канцелярита, рекламной интонации, AI-филлера и повторов.\n- В каждом разделе image_caption описывает только игровой скриншот, не artwork.\nКАНОНИЧЕСКАЯ ИДЕНТИЧНОСТЬ:\n${JSON.stringify(identity)}\nSOURCE DOSSIERS:\n${JSON.stringify(sourceDigest)}`;
function metrics(a){const sections=Array.isArray(a.sections)?a.sections:[],total=countWords([a.lead,...sections.flatMap(s=>s.paragraphs||[]),a.verdict?.summary].join(' '));return{sections,total,perSection:sections.map(s=>({id:s.id,words:countWords((s.paragraphs||[]).join(' ')),paragraphs:(s.paragraphs||[]).length,sources:new Set(s.source_ids||[]).size}))}}
function deterministicErrors(a){const valid=new Set(sourceDigest.map(s=>s.id)),m=metrics(a),errors=[];if(m.total<minWords)errors.push(`words ${m.total}/${minWords}`);if(m.total>maxWords)errors.push(`words ${m.total}>${maxWords}`);if(m.sections.length<minSections||m.sections.length>maxSections)errors.push(`sections ${m.sections.length}/${minSections}-${maxSections}`);for(const row of m.perSection){if(row.words<minSectionWords)errors.push(`${row.id}: words ${row.words}/${minSectionWords}`);if(row.paragraphs<minParagraphs)errors.push(`${row.id}: paragraphs ${row.paragraphs}/${minParagraphs}`);if(row.sources<minSectionSources)errors.push(`${row.id}: sources ${row.sources}/${minSectionSources}`)}const used=new Set(m.sections.flatMap(s=>(s.source_ids||[]).filter(id=>valid.has(id))));if(used.size<minUsed)errors.push(`materially used sources ${used.size}/${minUsed}`);for(const section of m.sections){for(const id of section.source_ids||[])if(!valid.has(id))errors.push(`${section.id}: unknown source ${id}`)}const latin=lowerLatin([a.lead,...m.sections.flatMap(s=>s.paragraphs||[]),a.verdict?.summary].join(' '));if(latin.length)errors.push(`lowercase latin intrusions: ${[...new Set(latin)].slice(0,20).join(', ')}`);return errors}
async function generate(extra=''){return chatJson({system:'Ты старший русскоязычный игровой журналист. Пишешь оригинальный большой журнальный текст только по предоставленным профессиональным досье и не додумываешь факты.',prompt:`${basePrompt}${extra}`,schema,temperature:0.18,numCtx:32768,numPredict:11000,timeoutMs:1500000})}
let article=null,errors=[];for(let attempt=1;attempt<=3;attempt++){article=await generate(attempt===1?'':`\nПредыдущая версия не прошла формальный gate: ${errors.join('; ')}. Перепиши целиком и исправь каждый пункт без снижения фактической точности.`);errors=deterministicErrors(article);if(!errors.length)break}if(errors.length)throw new Error(`${slug}: local long review deterministic gate failed: ${errors.join('; ')}`);
const auditSchema={type:'object',additionalProperties:false,required:['natural_russian','interesting_editorial_voice','source_grounding','specificity','balanced_criticism','no_generic_filler','issues'],properties:{natural_russian:{type:'boolean'},interesting_editorial_voice:{type:'boolean'},source_grounding:{type:'boolean'},specificity:{type:'boolean'},balanced_criticism:{type:'boolean'},no_generic_filler:{type:'boolean'},issues:{type:'array',items:{type:'string'}}}};
async function audit(a){return chatJson({system:'Ты строгий выпускающий редактор и фактчекер. Не пропускай машинный русский, повторы, общие слова и неподтверждённые факты.',prompt:`Проверь статью по досье. Все шесть булевых полей могут быть true только если статья естественная, конкретная, сбалансированная, без AI-филлера и каждое существенное утверждение реально поддерживается указанными source_ids.\nARTICLE:\n${JSON.stringify(a)}\nDOSSIERS:\n${JSON.stringify(sourceDigest)}`,schema:auditSchema,temperature:0.03,numCtx:32768,numPredict:2200,timeoutMs:900000})}
let auditResult=await audit(article),auditPassed=()=>['natural_russian','interesting_editorial_voice','source_grounding','specificity','balanced_criticism','no_generic_filler'].every(k=>auditResult?.[k]===true);if(!auditPassed()){for(let attempt=1;attempt<=2&&!auditPassed();attempt++){article=await generate(`\nВыпускающий редактор отклонил версию. Исправь замечания: ${(auditResult.issues||[]).join('; ')}.`);errors=deterministicErrors(article);if(errors.length)continue;auditResult=await audit(article)}}if(!auditPassed())throw new Error(`${slug}: local editorial audit failed: ${(auditResult.issues||[]).join('; ')}`);
const m=metrics(article),score=Number(reviews?.review_score?.calculation?.score_10??ratings?.calculation?.score_10??ratings?.score??game.ratings?.igropoisk);if(!Number.isFinite(score))throw new Error(`${slug}: canonical score missing`);const valid=new Set(sourceDigest.map(s=>s.id));for(const section of article.sections)section.source_ids=[...new Set(section.source_ids||[])].filter(id=>valid.has(id));const used=[...new Set(article.sections.flatMap(s=>s.source_ids||[]))];
const output={schema_version:12,slug,game_slug:slug,game_id:game.game_id||game.identity?.game_id||null,title:article.title,dek:article.dek,author:'Редакция Игропоиска',published_at:new Date().toLocaleDateString('ru-RU',{day:'numeric',month:'long',year:'numeric'}),updated_at:new Date().toISOString(),score,hero:game.media?.hero||game.media?.artwork?.[0]?.url||game.media?.artwork?.[0]||game.media?.cover||'',lead:article.lead,reading_time_minutes:Math.max(10,Math.ceil(m.total/190)),publication_status:'awaiting_media',source_gate:{preferred_full_reviews:preferred,accepted_full_reviews:sourceDigest.length,materially_used:used.length,exhaustive_discovery:exhaustive,passed:used.length>=minUsed},source_coverage:{available:sourceDigest.length,materially_used:used.length,preferred_target_met:sourceDigest.length>=preferred,exhaustive_discovery:exhaustive},methodology:sourceDigest.length>=preferred?`Материал основан на ${sourceDigest.length} независимых полнотекстовых профессиональных рецензиях.`:`Материал основан на всех ${sourceDigest.length} полнотекстовых профессиональных рецензиях, найденных после документированного исчерпывающего поиска.`,identity:{title:identity.title,developer:identity.developers.join(', '),publisher:identity.publishers.join(', '),release_date:game.release?.canonical_date_text||game.release?.date_text||'',genres:identity.genres,platforms:identity.platforms},sections:article.sections,verdict:article.verdict,used_source_ids:used,sources:sourceDigest.map(s=>({id:s.id,name:s.publication,title:s.title,url:s.url,body_words:s.body_words,purpose:'Полнотекстовая профессиональная рецензия, прочитанная и превращённая в source dossier'})),generation:{provider:'local-ollama-fallback',model:LOCAL_EDITORIAL_MODEL,checked_at:new Date().toISOString(),commercial_contract:contract.id,words:m.total,sections:m.sections.length,editorial_audit:{passed:true,...auditResult}}};write(`data/article-drafts/${slug}.json`,output);write(`data/articles/${slug}.json`,output);write(`data/parser-runs/review-synthesis-${slug}.json`,{parser:'commercial-long-review-resilient-v3',status:'green-awaiting-media',game_slug:slug,checked_at:new Date().toISOString(),provider:'local-ollama-fallback',model:LOCAL_EDITORIAL_MODEL,words:m.total,sections:m.sections.length,full_review_sources:sourceDigest.length,materially_used_sources:used.length,preferred_target_met:sourceDigest.length>=preferred,exhaustive_discovery:exhaustive,audit:auditResult});console.log(JSON.stringify({slug,status:'green-awaiting-media',provider:'local-ollama-fallback',model:LOCAL_EDITORIAL_MODEL,words:m.total,sections:m.sections.length,sources:sourceDigest.length,used:used.length},null,2));
