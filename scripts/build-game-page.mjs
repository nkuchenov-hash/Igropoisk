#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';
import {generateFreeEditorialJSON,assertFreeEditorialAI} from './lib/free-editorial-ai.mjs';

const root=process.cwd();
const slug=String(process.argv[2]||'').trim();
if(!slug)throw new Error('Usage: node scripts/build-game-page.mjs <slug>');
const read=(p,f=null)=>{try{return JSON.parse(fs.readFileSync(path.join(root,p),'utf8'))}catch{return f}};
const write=(p,v)=>{const t=path.join(root,p);fs.mkdirSync(path.dirname(t),{recursive:true});fs.writeFileSync(t,JSON.stringify(v,null,2)+'\n')};
const clean=v=>String(v||'').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim();
const cyrillicRatio=v=>{const s=clean(v);const letters=(s.match(/[A-Za-zА-Яа-яЁё]/g)||[]).length;return letters?(s.match(/[А-Яа-яЁё]/g)||[]).length/letters:0};
const draft=read(`data/drafts/${slug}.json`),parser=read(`data/parser-output/${slug}.json`,{}),corpus=read(`data/game-sources/${slug}.json`,{}),ratings=read(`data/ratings/${slug}.json`,{});
if(!draft?.identity?.title)throw new Error(`Missing draft for ${slug}`);
const sources=(corpus?.sources||[]).map(s=>({publication:s.publication||s.name||s.source_name||'',title:s.title||'',kind:s.kind||'',score:s.score??s.original_score??null})).filter(s=>s.publication||s.title).slice(0,40);
if(!corpus?.discovery?.complete||!sources.length)throw new Error(`${slug}: completed canonical source discovery and a non-empty source corpus are required before editorial repair`);
const ai=await assertFreeEditorialAI();
const forbidden=[/построена как .*разработчик/i,/основной режим рассчитан/i,/ключевые механики здесь выводятся/i,/официальное описание указывает/i,/описание не добавляет/i,/структура прохождения описывается/i,/информация о которой собрана/i,/проверяемых каталогов/i,/official sources|verified catalogs/i];
const normalize=editorial=>({short_description:clean(editorial?.short_description),integrated_description:clean(editorial?.integrated_description),campaign:clean(editorial?.campaign),features:Array.isArray(editorial?.features)?editorial.features.map(clean).filter(Boolean):[]});
const issuesFor=editorial=>{const issues=[];if(editorial.short_description.length<90)issues.push(`short_description слишком короткое: ${editorial.short_description.length}/90`);if(cyrillicRatio(editorial.short_description)<0.55)issues.push('short_description должно быть полноценным русским текстом');if(editorial.integrated_description.length<280)issues.push(`integrated_description слишком короткое: ${editorial.integrated_description.length}/280`);if(cyrillicRatio(editorial.integrated_description)<0.55)issues.push('integrated_description должно быть полноценным русским текстом');if(editorial.campaign.length<120)issues.push(`campaign слишком короткое: ${editorial.campaign.length}/120`);if(cyrillicRatio(editorial.campaign)<0.55)issues.push('campaign должно быть полноценным русским текстом');if(editorial.features.length<4)issues.push(`features: требуется минимум 4, получено ${editorial.features.length}`);editorial.features.forEach((item,index)=>{if(item.length<18)issues.push(`feature #${index+1} слишком короткая/общая: ${item.length}/18`)});for(const rx of forbidden){if(rx.test(editorial.short_description))issues.push(`short_description содержит запрещённый шаблон: ${rx}`);if(rx.test(editorial.integrated_description))issues.push(`integrated_description содержит запрещённый шаблон: ${rx}`);if(rx.test(editorial.campaign))issues.push(`campaign содержит запрещённый шаблон: ${rx}`)}return issues};
const fieldIssues=(editorial,field)=>issuesFor(editorial).filter(issue=>issue.startsWith(field)||(field==='features'&&/^(features|feature #)/.test(issue)));
const unwrapRepairValue=(value,field,depth=0)=>{if(depth>5||value===undefined||value===null)return value;if(typeof value==='string'||typeof value==='number'||typeof value==='boolean')return value;if(Array.isArray(value))return value;if(typeof value!=='object')return value;if(Object.prototype.hasOwnProperty.call(value,field))return unwrapRepairValue(value[field],field,depth+1);for(const key of field==='features'?['features','items','list','values','content','text','value']:['text','value','content','result','translation','description'])if(Object.prototype.hasOwnProperty.call(value,key))return unwrapRepairValue(value[key],field,depth+1);const values=Object.values(value);if(values.length===1)return unwrapRepairValue(values[0],field,depth+1);if(field==='features'){const arrays=values.filter(Array.isArray);if(arrays.length===1)return arrays[0];const strings=values.filter(item=>typeof item==='string'&&clean(item));if(strings.length>=4)return strings}return value};
const singleRepairValue=(raw,field)=>unwrapRepairValue(raw,field);
const mergeRepair=(current,raw,field)=>{const next={...current,features:[...current.features]};const value=singleRepairValue(raw,field);if(field==='features'){if(Array.isArray(value))next.features=value.map(item=>typeof item==='object'?clean(unwrapRepairValue(item,'text')):clean(item)).filter(Boolean);else if(typeof value==='string')next.features=value.split(/\n|[•;]+/).map(clean).filter(Boolean)}else if(value!==undefined&&value!==null&&typeof value!=='object')next[field]=clean(value);return next};
const deriveShortFromIntegrated=value=>{const text=clean(value);if(!text)return'';const sentences=(text.match(/[^.!?]+[.!?]?/g)||[]).map(clean).filter(Boolean);let out='';for(const sentence of sentences){const candidate=clean(out?`${out} ${sentence}`:sentence);if(candidate.length>178)break;out=candidate;if(out.length>=100)break}if(out.length<90){out=text.slice(0,177);const cut=out.lastIndexOf(' ');if(cut>=90)out=out.slice(0,cut);out=out.replace(/[,:;—-]+$/,'').trim()}if(out.length>178){out=out.slice(0,177);const cut=out.lastIndexOf(' ');if(cut>=90)out=out.slice(0,cut);out=out.replace(/[,:;—-]+$/,'').trim()}if(out&&!/[.!?]$/.test(out))out+='.';return out};
const facts={title:clean(draft.identity?.title),release:clean(draft.release?.date||draft.release?.date_text),developers:(draft.companies?.developers||[]).map(clean).filter(Boolean),publishers:(draft.companies?.publishers||[]).map(clean).filter(Boolean),genres:(draft.classification?.genres||[]).map(clean).filter(Boolean),platforms:(draft.classification?.platforms||[]).map(clean).filter(Boolean),categories:(draft.classification?.categories||[]).map(clean).filter(Boolean)};
const seed={short_description:clean(parser?.editorial?.short_description||draft.editorial?.short_description),integrated_description:clean(parser?.editorial?.integrated_description||draft.editorial?.integrated_description),features:(parser?.editorial?.features||draft.editorial?.features||[]).map(clean).filter(Boolean)};
const compactEvidence=JSON.stringify({facts,official_editorial_seed:seed});
const guidance={
  short_description:'Переведи смысл official_editorial_seed.short_description на естественный русский язык. Верни short_description длиной 100–180 символов. Не оставляй английские предложения.',
  integrated_description:'Напиши integrated_description на русском: 320–500 символов, цельный абзац о сути игры и подтверждённых механиках.',
  campaign:'Напиши campaign на русском: 130–220 символов о подтверждённой структуре/прогрессии игры. Если отдельной кампании нет, описывай только структуру развития, явно следующую из официального текста.',
  features:'Верни features как массив ровно из 4 конкретных русских фраз, каждая не короче 18 символов.'
};
const tokenBudget={short_description:180,integrated_description:360,campaign:220,features:300};
let next=normalize({short_description:seed.short_description,integrated_description:seed.integrated_description,campaign:'',features:seed.features});
let provider=ai.provider,model=ai.model,attemptLog=[],attempt=0;
for(const field of ['short_description','integrated_description','campaign','features']){
  for(let fieldAttempt=1;fieldAttempt<=2;fieldAttempt++){
    const currentIssues=fieldIssues(next,field);
    if(!currentIssues.length)break;
    attempt++;
    const prompt=`Игра: ${facts.title}. ${guidance[field]} Верни только JSON ровно с одним ключом ${field}. Не используй знания модели и не добавляй факты, которых нет в ДАННЫХ. Не пиши о парсинге, источниках или ИИ. Ошибки текущего значения: ${currentIssues.join('; ')}. ДАННЫЕ:${compactEvidence}`;
    const generated=await generateFreeEditorialJSON({system:'Ты русскоязычный игровой редактор. Весь редакционный текст пиши только по-русски. Возвращай только валидный JSON без пояснений.',prompt,temperature:0.05,maxTokens:tokenBudget[field],numCtx:2048});
    provider=generated.provider;model=generated.model;
    next=mergeRepair(next,generated.data,field);
    attemptLog.push({attempt,field,field_attempt:fieldAttempt,provider:generated.provider,model:generated.model,issues:fieldIssues(next,field),metrics:{short_description_chars:next.short_description.length,integrated_description_chars:next.integrated_description.length,campaign_chars:next.campaign.length,features:next.features.length}});
    if(!fieldIssues(next,field).length)break;
  }
}
if(fieldIssues(next,'short_description').length&&!fieldIssues(next,'integrated_description').length){const derived=deriveShortFromIntegrated(next.integrated_description);if(derived){next.short_description=derived;attemptLog.push({attempt:++attempt,field:'short_description',field_attempt:'derived-from-validated-integrated',provider:'deterministic',model:'derived-from-integrated',issues:fieldIssues(next,'short_description'),metrics:{short_description_chars:next.short_description.length,integrated_description_chars:next.integrated_description.length,campaign_chars:next.campaign.length,features:next.features.length}})}}
const issues=issuesFor(next),checkedAt=new Date().toISOString();
if(issues.length){write(`data/parser-runs/page-editorial-generation-${slug}.json`,{parser:'game-page-editorial-builder',status:'needs_revision',game_slug:slug,checked_at:checkedAt,provider,model,paid_api_required:false,repair_attempts:attemptLog.length,attempts:attemptLog,source_corpus:`data/game-sources/${slug}.json`,source_count:sources.length,official_editorial_seed_used:true,output:`data/drafts/${slug}.json`});throw new Error(`Editorial AI remained invalid after ${attemptLog.length} targeted attempt(s): ${issues.join('; ')}`)}
draft.editorial={...(draft.editorial||{}),...next};draft.publication={...(draft.publication||{}),status:'needs_revision',public_ready:false,quality_status:'editorial_generated_pending_qc'};draft.updated_at=checkedAt;write(`data/drafts/${slug}.json`,draft);write(`data/parser-runs/page-editorial-generation-${slug}.json`,{parser:'game-page-editorial-builder',status:'completed_pending_qc',game_slug:slug,checked_at:checkedAt,provider,model,paid_api_required:false,repair_attempts:attemptLog.length,attempts:attemptLog,source_corpus:`data/game-sources/${slug}.json`,source_count:sources.length,official_editorial_seed_used:true,output:`data/drafts/${slug}.json`});console.log(JSON.stringify({slug,status:'completed_pending_qc',provider,model,paid_api_required:false,sources:sources.length,repair_attempts:attemptLog.length,official_editorial_seed_used:true,public_ready:false},null,2));
